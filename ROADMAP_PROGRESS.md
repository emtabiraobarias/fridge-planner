# Roadmap Progress — fridge-planner

> Working log for the post-migration reconciliation + polish roadmap.
> Update the **two lines at the top** at the end of every session. That's the whole system.

**▶ NEXT ACTION (updated 2026-07-18):** **🎉 spec 005 intelligent quick-add is SHIPPED — app `4.3.0` live on Stage-1 prod, user-verified 2026-07-18.** Next feature: **backlog #2 — inventory-grounded meal consumption** via `/speckit.specify` (spec-first on `main`, implement on `impl/nextjs`; the new `ingredient_aliases` collection was named for its reuse). Then the next feature is **backlog #2 (inventory-grounded meal consumption)** via `/speckit.specify`. **The public production deployment stays parked at the BOTTOM** (internal Stage-1 prod at `https://fridgeplanner.lan:8443` serves all current needs — app `4.3.0`, agents `1.2.0`/`1.0.1`).

**Priority backlog (top = next):**
1. **✅ DONE — Intelligent quick-add understanding = spec `005` (specified 2026-07-17, BUILT + VERIFIED on `impl/nextjs` 2026-07-18; **SHIPPED to Stage-1 prod as app `4.3.0` same day, user-verified**).** Full spec-first chain ran: spec `005-intelligent-quick-add` on `main` (FR-IQ-001..022, 4 prioritized stories = the 4 tiers below) → per-branch plan/research/data-model/contracts → 35 tasks → `/speckit.analyze` (0 critical; A1/U1/U2 remediations applied) → implemented ALL FOUR stories on `impl/nextjs`: **IQ1** deterministic parser (locations, unit synonyms, expiry vocab + year rollover, trailing qty, comma multi-add; 47 corpus tests), **IQ2** tap-to-correct provenance chips (shared `ParsePreview`, override lib), **IQ3** per-user `ingredient_aliases` (GET/PUT `/api/v1/quick-add/aliases`, learned merge + one-tap expiry suggestions), **IQ4** fail-open AI assist (`POST /api/v1/quick-add/parse`, gpt-4o-mini, 1h cache, 20/min, 503-silent). Verified: **49 unit test files green, lint clean, `next build` clean, Playwright 11/11 (+2 screenshots), `validate-e2e --no-agent` 10/10**. Canonical parser doc (spec 004 `reference-logic.md` §1) revised per FR-IQ-009. `impl/vite` DEFERRED by decision as usual. *(Original scope analysis preserved below in git history.)* Make the natural-language quick-add reliably extract **name / quantity+unit / expiry / location** from free text. Analysis done 2026-07-16 on `impl/nextjs` against `packages/client/src/lib/quick-parse.ts` + spec 004 `design/reference-logic.md`. Known gaps: location is never parsed from text (only inferred via category guess — "chicken in the freezer" → fridge + polluted name); unknown unit words corrupt the name ("500 grams mince" → "Grams Mince"); expiry needs the literal `expires/exp` keyword (no `use by`/`best before`/`today`/`tomorrow`/month names; past `dd/mm` doesn't roll to next year); no trailing quantity ("milk 2L"); no multi-item input; preview chips are display-only; no learning from corrections. Recommended scope tiers: **(1)** deterministic parser extensions (explicit location tokens w/ override, unit synonym table, expiry keyword/token expansion, trailing qty, comma multi-add) · **(2)** tap-to-correct preview chips + guessed-vs-parsed confidence styling · **(3)** learned per-user alias memory (name→category/location/unit/typical shelf-life) + expiry suggestions · **(4)** optional AI-assist fallback for low-confidence parses (`POST /api/v1/inventory/parse`, Zod-gated to the enums, cached + rate-limited; plain structured-output call — no Holodeck needed). ⚠ The parser algorithm is **canonical in spec 004 `reference-logic.md`** and shared with grocery quick-add → draft via `/speckit.specify` (new spec `005` or an 004 revision) and cascade per CLAUDE.md §11. Spec-first on `main`, implement on `impl/nextjs`, release by tag + Portainer redeploy.
2. **🆕 QUEUED — Inventory-grounded meal consumption: ingredient↔inventory mapping + deduct-on-cooked (added 2026-07-16, spec change).** Closes the plan→cook→inventory loop in two stages; feasibility + design analysis done 2026-07-16 on `impl/nextjs`. **The limitation:** recommended meals carry free-text `usesIngredients`; consumption (`src/server/lib/ingredient-consumption.ts`) matches inventory by **exact case-insensitive name** with a **fixed 1-unit decrement** — "chicken breast" never matches "chicken", amounts/units are ignored, restore can't resurrect an item consumed to deletion, and deduction fires at **plan** time (so inventory lies until the meal is actually cooked, and the grocery generator double-counts what planning already deducted). **Stage 1 — map recommended meals to real inventory:** the meal-recommender already receives the inventory JSON (`_id`/quantity/unit), so its output schema + prompt change to return **inventory-grounded quantified ingredients** `{inventoryItemId, name, quantityToConsume, unit}` (zero extra LLM calls); the server treats that as untrusted — validate IDs/quantities against **live** inventory (zod, clamp, `unit-normalizer`) with a tiered fallback: item-ID → existing `ingredient-matcher` fuzzy → cached LLM alias lookup (cheap structured-output call on the existing `OPENAI_API_KEY`, persisted per-pair in a Mongo `ingredient_aliases` table). ⚠ **No embeddings/vector store** (CLAUDE.md §14). **Stage 2 — deduct when cooked, not when planned:** add `status: 'planned'|'cooked'` + `cookedAt` to meal-plan entries; completion endpoint `PATCH /meal-plans/:weekStart/entries/:slotId` with an **atomic planned→cooked guard** (idempotent, no double-deduct); move `consumeIngredients` out of `addMealEntry` into completion; make DELETE-restore + PUT net-diff **status-aware**; write **consumption receipts** (`consumedItems[]` incl. deleted-item snapshot) so un-cook restores exactly; "Mark cooked" action in the calendar detail modal + cooked tile state; invalidate the recs cache on completion (consumption bypasses the inventory controller's invalidation today); cutover = mark pre-existing entries `cooked` (their deduction already happened). **Spec impact:** amends FR-005 (consumption timing + semantics) and revisits the FR-027/FR-028 deferral (SG-03) — spec-first on `main` via `/speckit.specify` (new spec or `001` revision), cascade per §11, implement on `impl/nextjs`. Supersedes the Phase-2+ "consumption at checkout/mark cooked" deferral note below.
3. **🆕 QUEUED — Bought grocery items flow into kitchen inventory at check-off (added 2026-07-17, spec change; pairs with #2).** **The gap:** ticking a grocery item bought (`PATCH /grocery-lists/:weekStart/items/:itemId` → `isPurchased`) is **display-only** — inventory is touched solely by the FR-032 bulk checkout (`POST …/complete`, the "Done shopping" button), so a mid-shop check-off never reaches the Kitchen and skipping checkout loses the additions entirely. **Feature:** checking an item "bought" adds it to inventory immediately; un-checking reverses it. Scope notes from analysis (2026-07-17, `impl/nextjs`): server-side add on the purchase transition with an **atomic `isPurchased` false→true guard** (idempotent, no double-add on retry); store the created `inventoryItemId` on the grocery item as a **purchase receipt** (mirrors #2's consumption receipts) so un-check deletes/decrements exactly and FR-032 checkout **skips already-added items** (checkout becomes "mark remaining + finalize"); **data gaps at check-off** — the inline flow has no location/expiry prompt (checkout's `completeItemSchema` client-supplies them), so default location via the `ingredient-categorizer` category→location mapping + optional expiry suggestion (synergy with #1 tier-3 per-user alias memory); **duplicate handling** — merge quantities into an existing same-name inventory item (EC-03 semantics) instead of creating duplicates. **Spec impact:** amends FR-032 (+ grocery-item model gains the receipt field) → spec-first on `main` via `/speckit.specify`, cascade per §11, implement on `impl/nextjs`. Together with #2 this closes the full kitchen loop: **buy → inventory ↑ (this) · cook → inventory ↓ (#2)**.
4. **🆕 QUEUED — Daily rolling grocery-list refresh: recompute needs from today's remaining uncooked meals (added 2026-07-18, to be spec'd later; depends on #2, pairs with #3).** **The idea:** the grocery list should update **daily** to reflect what is **still missing from inventory** given the **remaining uncooked planned meals from today onwards** — meals dated **before today are ignored entirely, whether cooked or uncooked** (a skipped Tuesday dinner no longer generates shopping needs on Wednesday). **Dependencies:** needs spec `006`'s planned/cooked entry state (only `planned` entries count) and its quantity-aware netting (FR-MC-016 — owned stock netted off); pairs with #3 (check-off → inventory) — together the three keep the list live as the week progresses: **cook → needs shrink (#2) · buy → inventory grows (#3) · day rolls over → stale meals drop out (this)**. **Scope to decide at spec time:** date-scoped need computation (entry date ≥ today AND status `planned`); the refresh mechanism (recompute-on-view vs a scheduled daily regeneration — the list is lazily generated today, so on-view recompute may suffice); preservation of manual items + purchase check-off state across refreshes (FR-MC-019 / FR-030/031 semantics); and whether the week's list becomes a rolling "rest-of-week" view or keeps whole-week scope with past-meal exclusion. **Spec impact:** amends spec 001's grocery generation FRs (FR-025/026) and/or extends spec 006's Story 4 quantity model → spec-first on `main` via `/speckit.specify`, cascade per §11, implement on `impl/nextjs`.
5. **Quick hygiene wins (minutes each, do alongside #1):** GitHub branch protection requiring the `verify` check (E1 remainder) · EC-03 duplicate-merge prompt re-decision (dropped with the classic form in G2 — reinstate in QuickAdd or spec-tweak it away) · carried-forward doc chores (constitution §1 calendar-views line, stale `.specify/memory/constitution.md` pointers, `.env.local` docs).
6. **Phase 2+ product backlog (pick per appetite, spec-first):** ~~consumption at checkout/"mark cooked"~~ **→ promoted to #2 above (2026-07-16)** · true async 202+poll recommendations (#3/SG-02 follow-on) · quantity-aware groceries (FR-027/028 — largely absorbed by #2 Stage 1's quantified ingredients) · CR-013 OpenAPI spec (API shape is now stable).
7. **`impl/vite` decision:** keep-and-catch-up (owes Phase F, G, and the 2026-07 increment — see matrix) **or** formally sunset the branch. No work needed until decided.
8. **⏸ PARKED — LATER: Public production deployment bundle (Stage 2).** Grouped and deferred as one unit until a domain exists and public exposure is actually wanted: domain + DNS + router forward + Let's Encrypt + Keycloak public hostname + app-image rebuild with public `NEXT_PUBLIC_OIDC_*` (resume via `/deploy-runbook`) — **plus its operational companions:** E5 Redis-backed rate limiting (only matters >1 instance), E6 observability (Sentry/uptime/OTLP collector — worth doing before strangers use it). Nothing here blocks feature work.

*(Stage-1 history + E-task details remain in the Phase E section. Prior NEXT-ACTION text preserved below.)*

**(Prior NEXT ACTION, superseded 2026-07-16):** **Phase E — Production rollout & gated CI/CD** under way on `impl/nextjs` (deploy via a `nextjs-v*` tag, **no merge to `main`**); tasks **E0–E7** in the Phase E section + per-branch [`docs/deployment.md`](docs/deployment.md). **E0a ✅ DONE (2026-06-29, `216199d`):** the auth-client blocker is closed — the SPA now carries an OIDC access token (token store + `apiFetch` Bearer injection across all 15 service calls), `AuthContext` holds it + drives the login redirect (`NEXT_PUBLIC_OIDC_*`), and the `AuthBanner` 401 prompt has a Sign-in button; 9 new tests green, lint/build clean, coverage 93%. **E1 ✅ DONE** (`0193e91`, CI gate) and **E0b ✅ DONE** (`3178684`+`e3463ae`, OIDC PKCE callback). **🎉 Stage-1 INTERNAL production deployment COMPLETE + verified (2026-07-05)** — the full stack runs on one Portainer/TrueNAS node at `https://fridgeplanner.lan:8443` (Caddy edge + Next app + Mongo + Holodeck agent + Keycloak/Postgres), OIDC login round-trips, live AI recommendations work, `s8int-smoke` gate green. Three images published via CI (`fridge-planner-client:4.0.0-rc.5`, agent `fridge-planner:latest`, edge `fridge-planner-caddy:latest`); six bring-up fixes landed (standalone `server.js` path, `agent.serve.yaml`, baked Caddyfile, 8080/8443 port remap, post-login token race, fence-tolerant recs). **▶ NEXT (deferred by choice): Stage 2 — go public** (domain + DNS + router forward + Let's Encrypt + Keycloak public hostname + image rebuild for public `NEXT_PUBLIC_OIDC_*`); resume via `/deploy-runbook` (`deploy/checklist.yaml`) when a domain is ready. **(Phase D auth COMPLETE on both impls 2026-06-27.)** *(Other backlog: Phase 2+ deferrals — async-202 recs, quantity-aware grocery SG-03, checkout-time consumption.)*

**(Prior — Cb0–Cb4, ALL endpoints migrated to Next; Express proxy gone; 2026-06-24):** Express→Next Route Handler migration. **Cb0** Next server-layer scaffold (`db.ts` globalThis-cached Mongoose, deps, `@server/*` alias; `e57e82a`). **Cb1** inventory (thin handlers over extracted controller + `http.ts`; node-env + `mongodb-memory-server` harness; `fad36c7`). **Cb2** grocery-lists (6 routes incl. `complete`→inventory FR-032; grocery libs + grocery-list/meal-plan models; 16 tests; `71c5214`). **Cb3** meal-plans (reversible consume/restore preserved BUG #7/FR-005; added `src/server/logger.ts` to decouple `ingredient-consumption` from Express; 11 tests drive REAL consume/restore; `e395890`). **Cb4** recommendations LAST (`controllers/recommendations.ts` preserves the full fallback ladder — EC-01 empty→popular, cache hit, EC-08 agent-down→stale-cache-else-popular, never 500; copied meal-recommender + popular-recipes; **next.config proxy + proxyTimeout removed entirely**; 5 tests w/ mocked agent; `f627c48`). **Transition rule:** shared models/libs were **copied** into `src/server`; the duplication **collapses to a single source of truth when Express is deleted at Cb5**. Coverage gate restored earlier (78.41→81.29%; `d475dc4`→`4f8132e`). Suites green: server **199/199**, client **159/159**, lint clean. **Next — Cb5 (RETIRE EXPRESS):** remove `packages/server` from the dev script + docker-compose, drop `CORS_ORIGIN`, **reimplement the deferred middleware** (esp. the recommendations **10-req/min rate limiter** — express-rate-limit, not yet ported; endpoint currently unthrottled, mitigated by the 15-min cache) + decide a shared error wrapper (unhandled throw → Problem JSON), then delete `packages/server`. ⚠ **Two decisions to confirm before teardown:** (1) rate-limiter approach in Next; (2) delete `packages/server` outright vs. keep its Jest suite. Then **Cb6** doc cascade + version bump (→ 4.0.0).

> **Future roadmap (Phase 2+, deferred):** ~~move inventory consumption from **planning time** to **grocery-checkout / "mark cooked"** time (per #7 decision 2026-06-20)~~ — **promoted to priority-backlog #2 (2026-07-16)** as "inventory-grounded meal consumption" (ingredient↔inventory mapping + deduct-on-cooked); see the backlog entry above for the full scope.
**⏸ LAST LEFT OFF:** 2026-07-18 (later) — **queued "daily rolling grocery-list refresh" as priority-backlog #4** (grocery list recomputes daily from the remaining **uncooked planned meals dated today onwards**; meals before today ignored cooked-or-not; depends on #2's planned/cooked state + FR-MC-016 netting, pairs with #3; to be spec'd later). Same day: **spec `006-meal-consumption` drafted + clarified on `main` (FR-MC-001..020, 4 stories, cook-time consumption review) — draft PR #5 open**, awaiting review/merge before the FR-MC-020 cascade + per-branch plan on `impl/nextjs`. — *Prior:* 2026-07-18 — **🎉 spec 005 intelligent quick-add BUILT + VERIFIED on `impl/nextjs` (backlog #1 → DONE).** Full spec-first chain in two days: spec on `main` (`8f07911` + FR-IQ-002 units clarification `b35f569`) → plan/tasks/analyze on `impl/nextjs` → four story commits (`11ee27b` IQ1 parser, `ed9be2a` IQ2 chips, `ce7691b` IQ3 aliases, `f6184ef` IQ4 AI assist) + e2e (`8bd433d`) + doc cascade. Gates: 49 unit files / Playwright 11-of-11 / validate-e2e 10-of-10 / lint + build clean. **SHIPPED 2026-07-18:** tag `nextjs-v4.3.0` → CI build-push green → Portainer Pull-and-redeploy done → **live at `https://fridgeplanner.lan:8443`, user-verified (quick-add grammar, chips, alias learning working in prod)**. Quickstart smokes covered by the Playwright run (incl. IQ4 fail-open with no key). — *Prior:* 2026-07-17 — **queued "bought grocery items → kitchen inventory at check-off" as priority-backlog #3** (gap confirmed on `impl/nextjs`: per-item `isPurchased` PATCH is display-only, only FR-032 bulk checkout adds to inventory; scoped with purchase receipts, atomic toggle guard, categorizer-defaulted location, EC-03 merge; amends FR-032; pairs with #2 to close the buy→cook loop). — *Prior:* 2026-07-16 (later still) — **queued "inventory-grounded meal consumption" as priority-backlog #2** (analysis on `impl/nextjs`: exact-name/1-unit consumption limitation + LLM mapping design + deduct-on-cooked feasibility — two-stage scope in the backlog entry; supersedes the Phase-2+ "mark cooked" deferral, amends FR-005, revisits FR-027/028). — *Prior same day:* **parked "intelligent inventory quick-add understanding" as priority-backlog #1** (input-parsing gap analysis complete on `impl/nextjs`; ready for `/speckit.specify` — details in the backlog entry above). — *Prior same day:* **🎉 July increment SHIPPED TO PROD on `impl/nextjs` (app `4.2.1`, meal-rec agent `1.2.0`, feedback agent `1.0.1` — user-verified).** Spec deltas all authored on `main` (`impl/vite` inherits on sync, implementation **DEFERRED by decision** — see matrix): **FR-014** 5-10 candidate net; **FR-037** verified recipe links (evolved same-day from blocking drop/top-up/503 to **immediate results + async lazy links** via the new `POST /recommendations/verify-links`, settle-to-linked-only); spec 004 **FR-UI-019R** scoped inventory edit (expiry settable/clearable + location); spec 002 **FR-D-010** transparent token refresh (Keycloak realm now **12h idle/max**, set 2026-07-16). `impl/nextjs` code: FR-022 drag-rearrange + FR-024 details modal **restored** on the redesigned calendar (feedback 6a57677b); SuggestionsRail recipe-link bug; sequential Brave verification + 1h/5min per-name cache; EditItemSheet; single-flight refresh grant in `apiFetch` (feedback 6a56a2cc). **Both Holodeck agents now OpenAI** (feedback-collector migrated off Claude); `holodeck-base` 0.7.x routes `provider:openai` to the OpenAI Agents SDK backend → both agent Dockerfiles `pip install holodeck-ai[openai-agents]` (fixed the prod "No module named 'agents'" outage). **Phase F F6 ✅ DONE** (feedback agent live in prod). **CD decided:** git-backed **Portainer CE** stack → CI stops at build-push, manual Pull-and-redeploy (self-hosted-runner deploy job removed); release gate `validate-e2e` isolated onto `:3199`/`.next-e2e` (it had been silently smoking the dev server). Suites: **371 unit / 9-of-9 Playwright / 13-of-13 full gate.** — *Prior:* 2026-07-13 — **🎉 Phase G organic UI redesign COMPLETE + VERIFIED on `impl/nextjs` (G0–G5).** Distilled the external design handoff into a **self-contained** spec `004`, then implemented the full Organic redesign of all 4 screens + the bottom tab bar + smart quick-add, tap-to-place calendar, grocery inline checkout, and global toast — **frontend-only, no API/contract change** (SC-UI-008 ✓). **322 unit tests, coverage 91.15%, lint clean, `next build` clean, `validate-e2e --no-agent` 9/9, and a new Playwright suite (`e2e/redesign.e2e.ts`) 4/4 with 5 committed screenshots** (`packages/client/e2e/screenshots/`). Added `lucide-react` + `@playwright/test`. Deleted `design_handoff_organic_redesign/` (spec now carries everything). Fixed a quick-add expiry off-by-one (UTC-midnight anchor) surfaced by screenshot review. **The `impl/vite` implementation is DEFERRED by decision** (inherits spec `004` on sync). **Next (optional):** promote spec `004` (`spec.md` + `design/*` + `checklists/*`) to `main` so `impl/vite` inherits it; build the vite-side redesign when chosen. — *Prior:* 2026-07-11 — **Phase F feedback collector BUILT + VERIFIED on `impl/nextjs` (F0–F5).** Shared spec `003` on `main` (`FR-F-001..012`), then per-branch impl: second Holodeck agent `agents/feedback-collector/` (`:8002`, raw-JSON collecting/complete protocol, no web tools, injection-resistant), backend `/api/v1/feedback/**` (agent-fail→502 draft-preserved, ~30-turn finalize, 10/min chat), and the `/feedback` chat + review/export UI. **323 tests green, coverage 91.87%, `next build` clean, `validate-e2e --no-agent` 9/9, and a live agent smoke** (collecting / FINALIZE-complete / injection-resisted) all pass. Fixed 2 `holodeck-base:latest` drifts (no `setting_sources`; install Node.js). **Next:** F6 (prod second-agent container) is deferred Phase-E-style; the `impl/vite` implementation stays ☐ DEFERRED by decision. *(Phase E production track unchanged — Stage 2 still deferred by choice.)* — *Prior:* 2026-06-27 **🎉 Phase D COMPLETE on both impls + drift-check GREEN.** OIDC/JWT auth (spec `002`) implemented: `impl/nextjs` `3d0a3bb`/`69a415e` (Next server layer), `impl/vite` `40414d2` (Express middleware); suites green + E2E gates 9/9 both. Then ran the weekly drift-check: all shared docs **byte-identical across all 3 branches, sync-lag 0**; closed the two content gaps it found — added the shared **`AUTH-*` acceptance scenarios** to `checklists/acceptance-scenarios.md` and refreshed this log. **No phase queued** — candidate next directions listed in NEXT ACTION. *(Earlier this stretch: Phase D spec-first chain spec→plan→tasks→analyze; Phase C-bis COMPLETE v4.0.0.)*

---

## The roadmap at a glance

- **Phase A — Reconcile migration with the spec** (spec-tweak cascade) ✅ DONE
- **Branching strategy — two-impl model (Vite + Next.js)** ✅ DONE (2026-06-08; §5 steps 1–3: shared contract on `main`, branches renamed, both impls synced)
- **Phase B — Verify "complete" claims, triage rough edges** (bug vs spec-gap) — **both impls** ◀ NEXT
- **Phase C — Polish pass** (work the Phase B list, 1 issue/session) — **both impls** ✅ **DONE (2026-06-22)** — all 7 bugs + SG-01/02/03
- **Phase C-bis — Retire Express into Next Route Handlers** (optional architectural change; sequenced against `002`) — ✅ **COMPLETE (2026-06-27): Cb0–Cb6; Express retired, `packages/server` deleted, one Next process, v4.0.0**, **`impl/nextjs`-only**
- **Phase D — Spec `002` (authentication / real auth)** — ✅ **COMPLETE (2026-06-27): OIDC auth on BOTH impls** (nextjs Next server layer · vite Express middleware); same contract/lib/dev-seam, E2E gates 9/9 both. Real prod-auth deployment (a live IdP) is the only follow-on.
- **Phase E — Production rollout & gated CI/CD** (operational; deploy `impl/nextjs` via `nextjs-v*` tag, **no merge to `main`**) — ◐ **Stage-1 internal COMPLETE + in active use (releases through `4.3.0`); Stage 2 (public) PARKED at the bottom of the priority backlog (2026-07-16), `impl/nextjs`-only. E0 (auth client + PKCE callback) + E1 (CI gate) + E2 (CD build-push) ✅ DONE. 🎉 Stage-1 INTERNAL deployment COMPLETE + verified (2026-07-05)** on a single Portainer/TrueNAS node (`https://fridgeplanner.lan:8443`): Caddy + Next app + Mongo + Holodeck + Keycloak/Postgres, full OIDC login, live AI recs. **Stage 2 (public domain) deferred by choice.** See the Phase E section + per-branch `docs/deployment.md`.
- **Phase F — Feedback collector agent (spec `003`)** — ☑ **COMPLETE on `impl/nextjs` (F0–F6; F6 prod deploy ✅ 2026-07-15 — `fridge-planner-feedback:1.0.1`, OpenAI, live). Spec-level (shared on `main`); a conversational agent that gathers bug/improvement feedback and saves structured, spec-shaped records exportable as `/speckit.specify` input.** Built + verified on `impl/nextjs` (second Holodeck agent `:8002`, `/api/v1/feedback`, `/feedback` UI; 323 tests, live agent smoke green). **The `impl/vite` implementation is DEFERRED by decision** (spec inherited, not yet built — expected, not a drift finding). See the Phase F section + `specs/003-feedback-agent/`.
- **Phase G — Organic UI redesign (spec `004`)** — ☑ **BUILT + VERIFIED on `impl/nextjs` (G0–G5, 2026-07-13); `impl/vite` DEFERRED by decision. Spec-level (shared, promotable to `main`), frontend-only.** Restyles all four screens (Kitchen/Meal plan/Groceries/Feedback) onto the **Organic** design system (cream ground, terracotta+sage, Caprasimo/Figtree, heavily rounded) + two confirmed UX changes: a floating **bottom tab bar** and a natural-language **smart quick-add**. **No API/model/contract change** (FR-UI-030 / SC-UI-008). The self-contained spec (`specs/004-organic-redesign/` — `spec.md` + `design/organic-design-system.md` + `design/reference-logic.md`) replaces the external design handoff, which is **deleted** once implemented. **The `impl/vite` implementation is DEFERRED by decision** (inherits the shared spec on sync — like Phase F). See the Phase G section.

> **Phases B/C/D are spec-level → they apply to BOTH `impl/vite` and `impl/nextjs`** (per `BRANCHING_STRATEGY.md` §5). C-bis + Phase E are the plan-level exceptions (`impl/nextjs`-only). **Phase F is spec-level (shared spec 003) but its *implementation* is currently `impl/nextjs`-only by choice** — the `impl/vite` build is deferred, tracked as ☐ in the status matrix. See the "Phase B/C/D — both-implementation tracking" section below for the model + status matrix.

---

## Two-implementation branching strategy — execution state

See `specs/BRANCHING_STRATEGY.md` for the full model. Recommended sequence (§5) progress:

- [x] Step 1 — Shared spec content on `main` (spec.md + checklists/requirements.md verbatim; constitution.md genericized to v3.1.0; BRANCHING_STRATEGY.md + ROADMAP_PROGRESS.md as shared coordination layer).
- [x] Step 2 — Rename branches: `001-meal-planner`→`impl/vite`, `001-meal-planner-nextj-migrate`→`impl/nextjs` (local + origin; old remote branches deleted). `001-meal-planner-agent-refinement` left untouched (already merged via PR #4).
- [x] Step 3 — `main → impl/*` sync done (2026-06-08): merged `main` into both impl branches. Resolutions: constitution.md→main's genericized v3.1.0; spec.md→main's (propagated the A5 tightening to `impl/vite`); README.md + .gitignore kept per-branch; LICENSE+BRANCHING_STRATEGY+ROADMAP added. "Stack Realization" sections added to both `plan.md`s. **Sync convention going forward (Tier 1/2, 2026-06-08):** sync is now **conflict-free by construction** — run `bash scripts/sync-impls.sh` (or `git merge --no-edit main` per impl branch). Shared files (`spec.md`, `constitution.md`, `checklists/*`, `ROADMAP_PROGRESS.md`, `README.md`, `.gitignore`, `LICENSE`, `BRANCHING_STRATEGY.md`) are byte-identical on every branch and only edited on `main`; per-branch files (`plan.md`, `docs/DEVELOPMENT.md`, `CLAUDE.md`, `verification-findings.md`, code) never exist on `main`. No more `--ours`/`--theirs` judgement. See `BRANCHING_STRATEGY.md` §10.
- [ ] Step 4 — `impl/nextjs` proceeds to Phase B (verify) BEFORE C-bis changes the API topology. ◀ NEXT
- [ ] Step 5 — Phase B/C/D against the shared spec; route fixes per strategy §5 (spec-gap→`main`, bug→the branch where it occurs).

**Decisions carried (do not re-litigate, from §9):** shared = spec + criteria + checklist + constitution principles on `main`; per-branch = `plan.md` + code + concrete stack; C-bis is `impl/nextjs`-only; `002` auth spec stays topology-agnostic; merge to `main` of impl code deferred until all migration phases complete.

---

## Phase B/C/D — both-implementation tracking

Phases B (verify), C (polish), D (`002` auth) are **spec-level**, so each runs against **both** `impl/vite` and `impl/nextjs`. The model (decided 2026-06-08):

- **Shared layer (on `main`):** one canonical scenario list — [`checklists/acceptance-scenarios.md`](specs/001-meal-planner/checklists/acceptance-scenarios.md) with stable IDs (`US1-S4`, `EC-08`, `SC-014`, …) — plus the **spec-gap register** below. Both impls inherit on `git merge main`.
- **Per-branch layer (on each `impl/*`):** `specs/001-meal-planner/verification-findings.md` — that branch's pass/fail + bug log, referencing scenario IDs. Per-branch file (kept with `--ours` on sync).

**Finding-routing rule:**

| Finding | Fix where | Why |
|---|---|---|
| No scenario/FR covers it (**spec-gap**) | `spec.md` on `main` (+ add scenario to the shared checklist) | Contract gap — both impls owe it; inherited on sync |
| **Frontend** bug | the one branch where it occurs | Vite SPA vs Next SSR genuinely differ |
| **Backend / shared-logic** bug | **both** branches | `packages/server` (Express) is duplicated today → fix on one, cherry-pick to the other (until C-bis retires Express on `impl/nextjs`) |

**Tip:** walk the same feature area on both branches back-to-back — a failure on both is usually a spec-gap or a shared backend bug; a failure on one is usually frontend-specific.

### Phase status matrix

| Phase | `impl/vite` | `impl/nextjs` | Shared artifact |
|---|---|---|---|
| B — Verify | ☑ **confirmed** (2026-06-11, code-identity) | ☑ **all 4 areas done** (inventory/recs/calendar/grocery, 2026-06-08/11); **8 bugs, 3 spec-gaps** | scenario checklist + spec-gap register |
| C — Polish | ☑ **ALL #1–#7 ✔ + #3/SG-02** | ☑ **ALL #1–#7 ✔ + #3/SG-02** (latest `9a2c33e`); server **185/185**, client **118/118** | **Phase C COMPLETE** — #8→spec; SG-01/02/03 applied |
| D — `002` auth | ☑ **IMPLEMENTED** (`40414d2`): `authMiddleware` OIDC verify + dev seam (single `/api/v1` seam), `errorHandler`→401, SPA 401 UX; +20 tests; E2E gate 9/9 | ☑ **IMPLEMENTED** (`3d0a3bb`+`69a415e`): `authenticate()` OIDC verify + dev seam, 401 via withRoute, 12 handlers, public health, client 401 UX; +19 tests; E2E gate 9/9 | ☑ **spec `002` on `main`** (topology-agnostic; `FR-D-001..009`) |
| F — `003` feedback agent | ☐ **DEFERRED by decision** (spec inherited on sync; implementation not started — expected, not a drift finding) | ☑ **COMPLETE incl. F6** (F0–F5 `f74c5f5`→`ceb2a5f`; **F6 prod-deploy ✅ 2026-07-15**: `fridge-planner-feedback:1.0.1`, migrated to **OpenAI**, live in the Portainer stack) | ☑ **spec `003` on `main`** (topology-agnostic; `FR-F-001..012` + shared acceptance checklist) |
| G — `004` organic redesign | ☐ **DEFERRED by decision** (spec inherited on sync; frontend redesign not started — expected, not a drift finding) | ☑ **BUILT + VERIFIED** (G0–G5, 2026-07-13): Organic design system + bottom tab bar + smart quick-add across all 4 screens; frontend-only (no API/contract change, SC-UI-008 ✓); 322 tests, coverage 91.15%, `next build` clean, `validate-e2e --no-agent` 9/9, **Playwright 4/4 + 5 screenshots**. Handoff folder deleted. | ☑ **spec `004`** (topology-agnostic; `FR-UI-001..034`; self-contained `design/*`; promotable to `main`) |

| 2026-07 increment — recipe links (FR-014/037 + `verify-links`), inventory edit (FR-UI-019R incl. `expiresAt:null` API), auth refresh (FR-D-010), calendar FR-022/024 | ☐ **DEFERRED by decision** (specs inherited on sync). Owes: recs verify-links pipeline, clear-expiry backend path, FR-D-010 refresh; FR-022/024 likely already satisfied (pre-redesign calendar) — re-verify | ☑ **SHIPPED TO PROD `4.2.1`** (2026-07-16, user-verified; 371 unit / 9-of-9 e2e / 13-of-13 gate) | ☑ specs `001`/`002`/`004` revised on `main`; checklist section "2026-07 increment" |

*(Status legend: ☐ not started · ◐ in progress · ☑ done. Update per cell as each branch progresses.)*

### Spec-gap register (shared — fix on `main`, both inherit)

Findings with **no** covering scenario/FR. File the spec change on `main`, add the new scenario ID to the shared checklist, then check the box.

| ID | Found on | Description | Spec change | Status |
|----|----------|-------------|-------------|--------|
| SG-01 | impl/nextjs (inventory) | No FR/CR explicitly requires per-user data isolation — only implied by "my/their inventory" + CR-001. Surfaced by a live cross-user data-leak bug (`GET /inventory` ignores `userId`). | Propose an explicit FR (or tighten CR-001) on `main`; then both impls' isolation bugs trace to a testable requirement. | **DECIDED 2026-06-11: explicit FR + fix #1 now (both branches).** Add **FR-036** (all data ops scoped to authenticated user). Spec already asserts it in Key Entities→User + CR-001. |
| SG-02 | impl/nextjs (recommendations) | SC-002 "within 5 s" doesn't distinguish a **cached** hit from a **cold, web-researched** recommendation (measured **142 s** live — the agent does `WebSearch`/`WebFetch`). The 5 s target is unrealistic for the chosen architecture. Companion to `impl/nextjs` BUG #3. | Decide: re-architect for 5 s (drop web search / async job / stream) **or** revise SC-002 (e.g. split cached `<5 s` vs fresh `<N s`). Then edit SC-002 on `main`. | **DECIDED 2026-06-11: async UX + soften SC-002.** Recommendations become async (immediate loading state, delivered when ready); reword SC-002 as a UX/time-to-first-feedback criterion + mark the endpoint async (exempt from CR-008 <200ms). Async UX itself is per-branch Phase C. |
| SG-03 | impl/nextjs (grocery) | US3-S2/S3 + SC-005 assume meals carry ingredient **quantities/units** (e.g. "milk 1 cup", "6 eggs"), but `MealRecommendation.missingIngredients` is `string[]` (names only). So quantity-aware aggregation/normalization/deduction is impossible by design (BUG #8); grocery items are a meal-count in `'servings'`. | Decide: (a) extend the meal model — agent returns `{name, quantity, unit}` per ingredient — to meet US3-S2/S3/SC-005, or (b) revise those scenarios to the servings/count model. | **DECIDED 2026-06-11: revise spec to servings model.** Defer **FR-027** (inventory deduction) + **FR-028** (unit normalization) to Phase 2+; clarify FR-026 (aggregate by meal-count/servings); revise US3-S2/S3 + soften SC-005. Spec-only; BUG #8 then "matches spec". |

> **Phase B bugs are logged per-branch** in each branch's `verification-findings.md` (not here). The register above is only for *spec-gaps* (shared, fixed on `main`). Open bugs (found on `impl/nextjs`, **CONFIRMED on `impl/vite` 2026-06-11 via byte-identical server code**; #4 is *worse* on `impl/vite` — its `meal-recommender` lacks the 220s timeout): **~~#1~~ ✔ FIXED 2026-06-11** (FR-036; `userId` scoping on inventory GET/PUT/DELETE + recs; TDD'd; `impl/vite` `29d2e89` → cherry-picked `impl/nextjs` `532e198`), **~~#2~~ ✔ FIXED 2026-06-21** (EC-03 duplicate-merge prompt; Merge/Add-separately/Cancel; `7ba9c59`→`abf3088`), **~~#3~~ ✔ RESOLVED 2026-06-22** (SC-002/FR-012 async via SG-02; client surfaces fallback + immediate non-blocking loading; `b193323`→`9a2c33e`; true 202+poll deferred Phase 2+), **~~#4~~ ✔ FIXED 2026-06-19** (EC-08/SC-010 graceful fallback — stale-cache→popular, no more 500; `impl/vite` got the 220s timeout; `edfb0a9`→`3cc068d`), **~~#5~~ ✔ FIXED 2026-06-19** (EC-01 empty→popular recipes), **~~#6~~ ✔ FIXED 2026-06-19** (`expirationStatus` now date-derived on read — `expiration.ts` query builders + GET recompute; SC-014/US1-S9 pass; `impl/vite` `95afbe5` → `impl/nextjs` `41e9881`), **~~#7~~ ✔ FIXED 2026-06-20** (reversible consumption — POST consumes / DELETE restores / PUT net-diffs, awaited; FR-005 clarified; `c6bf4b8`→`7dca07a`), **#8** grocery is count-of-meals not quantity-aware — **now matches spec (SG-03 servings) → reclassified Phase-2+, not a bug**. **Remaining: #2** (EC-03 duplicate prompt, per-branch frontend) and the **async recommendation UX** (#3/SG-02, per-branch frontend).

> **SG-01/02/03 spec edits APPLIED to `spec.md` + `acceptance-scenarios.md` (2026-06-11):** added **FR-036** (data isolation); deferred **FR-027/FR-028** to Phase 2+; **FR-026** + **US3-S2/S3** + **SC-005** → servings model; **FR-012** + **SC-002** → async (exempt from CR-008). **Effect on bugs:** **#8 now matches spec** (servings is the contract → reclassify as Phase-2+ enhancement, not a bug). **Remaining code follow-ups (Phase C, per branch):** fix **#1** against the new FR-036 (both branches); build the **async recommendation UX** for SC-002 (per-branch client).

---

## Phase A — task checklist

- [x] A1 — `constitution.md`: stack, tooling, performance, version → 3.0.0
- [x] A2 — `specs/001-meal-planner/plan.md`: Technical Context, ports, architecture
- [x] A3 — `README.md`: ports (5173→3000), architecture diagram, client container, structure tree
- [x] A4 — `.env.example` + spec sweep for `5173`/Nginx refs; run `/speckit.analyze`
- [x] A5 — Commit Phase A doc edits on `impl/nextjs` (was `001-meal-planner-nextj-migrate`) + tag checkpoint `migration-docs-reconciled`. **Not merged** — merge deferred until all migration phases complete.

## Phase C-bis — Retire Express into Next Route Handlers

> Optional architectural change, **`impl/nextjs`-only** (plan-level, never touches the shared spec). Collapses the Express API (:3001) into Next.js Route Handlers so the app runs as one Node process. Plausibly a **major version bump (3.x → 4.0.0)** of the per-branch plan. Each step is ~1 session and ends runnable; Express keeps running until the last step removes it.
>
> **✅ Merge boundary DECIDED (2026-06-07):** option (b) — impl-code merge to `main` is deferred until ALL migration phases are complete. A5 commits + tags on the branch but does not merge. C-bis lands as a further self-consistent checkpoint on the SAME `impl/nextjs` branch.

- [x] Cb0 — Scaffold server layer: `src/server/db.ts` with `globalThis`-cached Mongoose connection (avoids dev hot-reload "model already compiled"); add `import 'server-only'` guards. No behavior change. **✅ 2026-06-23 `e57e82a`** — also added `mongoose`/`zod`/`server-only` deps + `@server/*` alias. (Models are copied in progressively per endpoint, not all up-front — Express still needs its copies until Cb5.)
- [x] Cb1 — Migrate **inventory** (pure CRUD, lowest risk): `app/api/v1/inventory/route.ts` + `[id]/route.ts`. Re-point only this path off the Express proxy in `next.config.ts`. Tests against the handlers. **✅ 2026-06-23 `fad36c7`** — thin handlers over extracted `src/server/controllers/inventory.ts` + `http.ts`; proxy uses an explicit allow-list (not a catch-all) so it can't shadow the `/inventory/[id]` dynamic route; node-env Vitest + `mongodb-memory-server` harness (`@server`/`server-only` aliased) covers CRUD + FR-036 isolation + BUG #6 derived status. Client 118→127.
- [x] Cb2 — Migrate **grocery-lists**, including nested `checkout`/`complete` action as its own `route.ts`. **✅ 2026-06-24 `71c5214`** — all six routes under `app/api/v1/grocery-lists/[weekStart]/` over `controllers/grocery-lists.ts`; copied grocery libs (generator/matcher/categorizer/unit-normalizer) + grocery-list & meal-plan models into `src/server`; 16 node-env handler tests (client 127→143). Note: `complete` ADDS bought goods to inventory (FR-032), so it does **not** touch `ingredient-consumption` — the logger wrinkle moved to Cb3.
- [x] Cb3 — Migrate **meal-plans** read/write only (NOT recommendations yet). **✅ 2026-06-24 `e395890`** — GET / `entries` POST / `entries/[slotId]` DELETE / PUT over `controllers/meal-plans.ts`; reversible consume/restore preserved (BUG #7/FR-005); added `src/server/logger.ts` (framework-neutral, pino-shaped) to **decouple `ingredient-consumption` from Express's `app.ts`**; 11 node-env tests drive real consume/restore vs seeded inventory (client 143→154).
- [x] Cb4 — Migrate **recommendations LAST** (only endpoint with an external dep — Holodeck :8001). **✅ 2026-06-24 `f627c48`** — `app/api/v1/recommendations/route.ts` (maxDuration=240) over `controllers/recommendations.ts`, preserving the full fallback ladder (EC-01 empty→popular · cache hit · EC-08 agent-down/timeout/malformed → stale-cache-else-popular, never 500); copied `services/meal-recommender.ts` (220s timeout) + `lib/popular-recipes.ts`; recommendations-cache already in `src/server` since Cb1; 5 node-env tests w/ mocked agent. **next.config proxy rewrites + proxyTimeout removed entirely.** ⚠ **Rate limiter NOT ported** (deferred to Cb5).
- [x] Cb5 — Retire Express (**soft-retire**, decisions: in-memory limiter + keep `packages/server` until real-boot). **✅ 2026-06-25** — **5a** `e95ee95` middleware ported (`src/server/rate-limit.ts` 10/min→429 + `withRoute` error wrapper on all 12 routes); **5c** `9d8c09b` stopped running Express (dev/build scripts + docker-compose + CORS/PORT/BACKEND_URL); **5b** `a2308d4` ported 88 pure-logic unit tests (Jest→Vitest). `next build` validates all handlers; `npm test` server 199 + client 252. **Deferred to follow-up:** `rm packages/server` (gated on real-boot smoke test) + porting the DB/fetch unit tests (already covered by handler tests).
- [x] Cb6 — Delete `packages/server` + doc cascade + version bump. **✅ 2026-06-27** — `70af3b9` deleted `packages/server` (real-boot gate passed → single source of truth); root `package.json` workspaces→[client], test→client-only, dropped `concurrently`, **version 0.0.1→4.0.0**; doc cascade `e926e68` (CLAUDE.md §1-14, plan.md Stack Realization, docs/DEVELOPMENT.md — Next Route Handlers, one process, `src/server` layout, inverted `.js`-import rule) + README on `main` `1414269`. Client 258/258, lint clean, `next build` + E2E gate green. **Phase C-bis COMPLETE.** Remaining: decide the impl→main merge.

**Sequencing vs Phase D (`002` auth):** same-origin Route Handlers + Next middleware make auth simpler than a split-origin Express API. If retiring Express at all, doing C-bis *before* D means building auth once on the consolidated architecture. C-bis is OPTIONAL — "Express stays, auth built on it" is defensible. This is why C-bis sits before D but is not a hard prerequisite: decide deliberately. **(Resolved: C-bis done first; Phase D now builds auth on the consolidated `impl/nextjs` architecture and the unchanged `impl/vite` Express.)**

## Phase D — Authentication (spec `002`)

> **Spec-level → applies to BOTH impls** (per `BRANCHING_STRATEGY.md §5`). This is the model in action: **one shared spec on `main`, two per-branch enforcements.** It is NOT `impl/nextjs`-only (that was C-bis). Replaces the `X-User-Id` dev stub with real OAuth 2.0/OIDC, building on `001`'s FR-036 / CR-001 / CR-002 / Assumption 12.

**Shared layer (on `main`, byte-identical, synced to both):**
- [x] **D0 — Spec drafted** ✅ 2026-06-27 — [`specs/002-authentication/spec.md`](specs/002-authentication/spec.md): topology-agnostic contract (`FR-D-001..009`, `CR-D-001..003`, `SC-D-001..004`) — validate Bearer JWT (JWKS + iss/aud/exp), identity from `sub`, FR-036 isolation → 404 cross-user, 401 Problem JSON, public health, dev/test seam.
- [x] **D1 — Acceptance scenarios + analyze** ✅ 2026-06-27 — `/speckit.analyze` cross-check done both impls (caught FR-D-009 gap; `c069e03`/`c1ccb63`); shared `AUTH-US1/2/3 + AUTH-EC/UX` scenarios added to `checklists/acceptance-scenarios.md` (all ☑ verified) during the weekly drift-check.

**Per-branch layer (each `impl/*` `plan.md` + code — same contract, different enforcement point):**
- [x] **`impl/nextjs` plan** ✅ `3d33d58` — `specs/002-authentication/plan.md`: verify in the **Next server layer** (`getUserId`→`async authenticate` via `jose`; `withRoute`→401; all 12 handlers `await`); dev/test seam; `jose` dep. Tasks/code TODO.
- [x] **`impl/vite` plan** ✅ `f208bdd` — `specs/002-authentication/plan.md`: rewrite the single **Express `authMiddleware`** (`app.use('/api/v1', …)`) → `jose` verify + `next(AuthError)`; `errorHandler`→401; health stays public. Tasks/code TODO.
  - Both plans: same `jose` lib, env (`AUTH_MODE`/`AUTH_ISSUER`/`AUTH_AUDIENCE`/`AUTH_JWKS_URI`), dev seam, 401/404 semantics — only the insertion point differs (cross-impl equivalence).
- [x] **Both `tasks.md` drafted** ✅ — impl/nextjs `3bced73` (T001–T012; 12-handler swap), impl/vite `8387923` (T001–T011; single-middleware seam, fewer tasks). TDD, FR-D traceability, `[P]` markers.
- [x] **`/speckit.analyze`** cross-check ✅ 2026-06-27 (both branches: `c069e03` nextjs, `c1ccb63` vite) — caught **A1 (HIGH): FR-D-009 had no task** → added a frontend task (`T011`/`T010`) + plan `D-NX-6`/`D-VT-5`; A2 tightened the cross-user test to all resource types; A3 added clock-skew + JWKS-rotation edge coverage; A4 (info) SC-D-003 latency = design-satisfied (no perf task). No spec↔plan↔tasks contradictions.
- [x] **Implement `impl/nextjs`** ✅ 2026-06-27 (`3d0a3bb` backend + `69a415e` UX/docs/gate) — `authenticate()` (jose OIDC verify + dev seam + prod guard), `AuthError`→401 via `withRoute`, all 12 handlers `await authenticate`, public `/api/health`, client `AuthBanner` 401 UX; +16 tests (verifier 9 + handlers 6 + banner 3, +guard); `validate-e2e --no-agent` 9/9 green.
- [x] **Implement `impl/vite`** ✅ 2026-06-27 (`40414d2`) — `authMiddleware` OIDC verify + dev seam at the single `app.use('/api/v1', …)` seam, `errorHandler`→401, SPA `AuthBanner` 401 UX; +20 tests (verifier 10 + integration 7 + banner 3); `validate-e2e --no-agent` 9/9. **Phase D COMPLETE on both impls.**

**Workflow (spec-first):** spec on `main` ✅ → per-impl `/speckit.plan` (enforcement design) → `/speckit.tasks` → `/speckit.analyze` → implement per branch (TDD; shared spec edits stay on `main` and sync down). **Out of scope:** token issuance, login UI, IdP configuration.

**Decision note:** proceeding with Phase D on **both** branches confirms **keeping the two-impl model** (no `impl→main` collapse). The old "merge impl→main once migration done" boundary is superseded by the two-impl topology.

## Phase E — Production rollout & gated CI/CD (`impl/nextjs`-only)

> **Operational / plan-level, `impl/nextjs`-only** (like C-bis — not a spec phase, never touches the shared contract). **Goal:** deploy `impl/nextjs` to production with gated CI/CD **without merging to `main`** — release by cutting a `nextjs-v*` tag off `impl/nextjs`; `main` stays contract-only. Detailed runbook + full prerequisites checklist live in the per-branch [`docs/deployment.md`](docs/deployment.md) (on `impl/nextjs`).
>
> **Runtime shape:** standalone Next container (`packages/client/Dockerfile`, one process `:3000`, serves UI + `/api/v1`) + managed MongoDB (Atlas) + Holodeck sidecar + OIDC IdP, behind TLS. Recommended host **Cloud Run** (matches the agent's GCP target; supports the recommendations route's **240 s** timeout — ⚠ Vercel cannot).

- [x] **E0 — Auth client wiring ✅ DONE on `impl/nextjs`.** **E0a** (`216199d`): token store + `apiFetch` Bearer injection (all 15 service calls), `AuthContext`, AuthBanner Sign-in. **E0b** (`3178684`+`e3463ae`): full authorization-code + PKCE (S256) flow — `login()` → Keycloak `/protocol/openid-connect/auth`, `app/auth/callback` exchanges the code (+ verifier) for a token; `NEXT_PUBLIC_OIDC_*` baked via Dockerfile build-args; post-login 401 race fixed by seeding the token at `http.ts` module load. **Verified against the live Stage-1 Keycloak** (login round-trips, authenticated data loads). Token refresh-on-expiry left as a future nicety.
- [x] **E1 — CI workflow ✅ DONE on `impl/nextjs`** (`0193e91`): `.github/workflows/ci-nextjs.yml` — on push/PR to `impl/nextjs` → `npm ci` → lint → `npm test` → `bash scripts/validate-e2e.sh --no-agent`. Node 20; **no Mongo service container** (the e2e script brings Mongo up via `docker compose`, avoiding a `:27017` clash); server tests use in-process `mongodb-memory-server`; `--no-agent` ⇒ no LLM creds. Verified locally: lint OK · 283 tests · e2e 9/9. Closes the long-standing "no CI" TODO. **⏳ Manual remainder:** enable branch protection requiring the `verify` check (GitHub repo-settings step).
- [x] **E2 — CD workflow ✅ DONE (final form 2026-07-15)** (`.github/workflows/deploy-nextjs.yml`): on tag `nextjs-v*` → digest-pinned build-push (`:<version>` + `:sha` + `:latest`) → **manual Portainer Pull-and-redeploy** (prod is a git-backed **Portainer CE** stack — the runner/SSH deploy job was removed; rollout steps print in the run summary; rollback = repoint `APP_IMAGE`). Proven through releases 4.1.1→4.2.1.
- [ ] **E3 — Infra prerequisites** (see `docs/deployment.md`): domain + DNS, TLS cert, container registry (GHCR/GAR), host (Cloud Run, request timeout ≥240 s), MongoDB Atlas, Holodeck sidecar deploy + LLM key, OIDC IdP registration (`AUTH_ISSUER`/`AUDIENCE`/`JWKS_URI`).
- [ ] **E4 — Secrets & prod env** in a secret manager (+ GH Actions secrets scoped to the `production` env): `NODE_ENV=production`, `AUTH_MODE=oidc` **and NOT `AUTH_ALLOW_DEV`** (that flag is E2E-only — would re-open the dev seam), `MONGODB_URI`, `HOLODECK_URL`, `AUTH_*`, LLM key(s). Never bake into the image.
- [ ] **E5 — Multi-instance rate limit:** the recommendations limiter is **in-memory (per instance)**; for >1 instance back it with Redis (`REDIS_URL` placeholder exists) or accept per-instance. Otherwise app is stateless (state in Mongo) → safe to scale.
- [ ] **E6 — Observability:** health checks → `/api/health`; centralized logs + error tracking (Sentry); uptime monitor; optional OTLP collector (agent already exports).
- [ ] **E7 — Release:** cut `nextjs-v4.0.0` from `impl/nextjs` → approve the gate → post-deploy verify. No merge to `main`.

> **Top risks:** (1) **E0 auth wiring** — without it oidc prod is unusable for humans; (2) per-instance rate limit (E5); (3) the 240 s recommendations timeout constrains the host (E3); (4) `AUTH_ALLOW_DEV` must never be set in prod (E4).

## Phase F — Feedback collector agent (spec `003`)

> **Spec-level phase (shared contract on `main`), but implemented on `impl/nextjs` first; the `impl/vite` implementation is DEFERRED by decision.** Adds a conversational feedback agent: users chat to report bugs / suggest improvements, an agent asks clarifying questions until it can emit a structured, spec-shaped record (persisted per-user), reviewable in-app and **exportable as markdown aligned with `.specify/templates/spec-template.md`** — ready to paste into `/speckit.specify`. This closes the spec-driven loop: user feedback → structured record → specification input. Requirements `FR-F-001..012`, success criteria `SC-F-001..005` in [`specs/003-feedback-agent/spec.md`](specs/003-feedback-agent/spec.md).
>
> **Runtime shape (`impl/nextjs`):** a **second Holodeck agent** `agents/feedback-collector/` (Claude Sonnet 4.6, no web tools, raw-JSON conversation protocol) on its own container/port (`:8002`, `FEEDBACK_AGENT_URL`) — one Holodeck instance serves one agent, mirroring `agents/meal-recommender/`. Backend: `/api/v1/feedback` route handlers over `controllers/feedback.ts` + a new `FeedbackRecord` model; **stateless transcript replay** (full conversation resent each turn — CR-018, survives restarts). Frontend: a `/feedback` chat page + own-records review/export.

- [x] **F0 — Shared spec `003` on `main`** — `spec.md` (`FR-F-001..012`, `SC-F-001..005`, US1 chat collection / US2 review+export / US3 resume-or-discard), `checklists/requirements.md` (quality gate, all pass), `checklists/acceptance-scenarios.md` (shared stable IDs). ROADMAP Phase F entry + status matrix (impl/vite ☐ deferred). Merge → `main` → `sync-impls.sh` (both impls inherit the spec).
- [x] **F1 — Plan + tasks on `impl/nextjs`** ✅ (`f74c5f5`) — per-branch `plan.md` (Holodeck second-agent + Next server layer, stateless transcript replay) + `tasks.md` (T001–T018, TDD, FR-F traceability); spec↔plan↔tasks self-checked (every FR-F maps to a task).
- [x] **F2 — Feedback agent** ✅ (`600e2a0`, `d0952f7`) — `agents/feedback-collector/` (`agent.yaml` + serve variant, raw-JSON protocol prompt, injection-resistant, no web tools, Dockerfile `:8002`, 3 evals + 3 test cases); `docker-compose.yml` `holodeck-feedback` + `FEEDBACK_AGENT_URL`. **Live-smoke verified** against the real LLM: vague opener → `collecting`; `FINALIZE NOW` → valid `complete` bug record; injection probe → protocol preserved. ⚠ **Base-image drift fixed:** current `holodeck-base:latest` rejects `claude.setting_sources` and no longer bundles Node.js → removed the field + `apt-get install nodejs` in the Dockerfile (the older meal-recommender image predates both).
- [x] **F3 — Backend** ✅ (`600e2a0`) — `models/feedback-record.ts`, `services/feedback-collector.ts` (framing + fence-strip + zod discriminated union), `controllers/feedback.ts` (start/continue/list/get/delete/export; agent-fail → 502 draft-preserved; ~30-turn finalize cap), `lib/feedback-export.ts`, `app/api/v1/feedback/**` (10/min chat rate-limit). Tests: export (5) + service (10) + handler suite (12).
- [x] **F4 — Frontend** ✅ (`8e8dd3f`) — `services/feedback.ts`, `FeedbackContext` (chat state machine + review list), `FeedbackPage` + `components/feedback/*` (chat list `role=log`, input Enter-to-send, completion card copy/download, history export/delete), `app/feedback/page.tsx`, nav link. Tests: context (6) + view (5).
- [x] **F5 — Verify** ✅ — `npm run lint` clean; **323 tests green**; coverage **91.87%** (feedback routes 100%); `next build` clean; `validate-e2e.sh --no-agent` **9/9**; **live agent smoke** green (see F2). *(`holodeck test` eval metrics need `CLAUDE_CODE_OAUTH_TOKEN` + `AZURE_OPENAI_*` — run manually; not in CI, by design.)*
- [x] **F6 — Prod deploy of the second agent container** — **✅ DONE 2026-07-15** (was deferred Phase-E-style) (`docker-compose.prod.yml`, `deploy/checklist.yaml`, CI image build, GHCR `…/fridge-planner-feedback`). Until then the feature runs only where `FEEDBACK_AGENT_URL` is configured; the controller 502-degrades cleanly elsewhere.

> **Deferred `impl/vite` implementation:** the shared spec `003` is inherited on `impl/vite` via sync, but no Vite-side code is planned yet — this is a deliberate choice (revisit later), **not** an unexplained gap. The weekly drift-check should treat `impl/vite` Phase F as ☐-deferred.
>
> **Top risks:** (1) transcript-replay token growth → bounded by the ~30-turn cap + forced finalize (FR-F-008); (2) prompt injection via user chat → marker framing + no tools + zod gate + reply-only rendering (FR-F-011); (3) `agent.serve.yaml` drift (no `${…}` in the serve file); (4) second prod container is out of scope here (F6).

## Phase G — Organic UI redesign (spec `004`)

> **Spec-level phase (shared contract on `main`), implemented on `impl/nextjs` first; the `impl/vite` implementation is DEFERRED by decision** — mirrors Phase F exactly. A full **visual + UX uplift** of all four screens onto the **Organic** design system, plus two confirmed UX changes: a floating **bottom tab bar** (replaces the header links) and a natural-language **smart quick-add** (replaces the multi-field inventory form). Requirements `FR-UI-001..034`, success criteria `SC-UI-001..008` in [`specs/004-organic-redesign/spec.md`](specs/004-organic-redesign/spec.md).
>
> **Frontend-only.** No new API endpoints, no data-model/contract change (FR-UI-030 / SC-UI-008) — every interaction maps onto existing endpoints; client-side parsing builds the payloads they already accept. Three existing flows are intentionally changed: inventory **edit-in-place removed** (stepper + delete), grocery **checkout modal → inline button**, drag-and-drop **demoted** to optional behind **tap-to-place** (FR-UI-019/029/026/031).
>
> **Self-contained spec (handoff deletable).** The external `design_handoff_organic_redesign/` folder was distilled into `specs/004-organic-redesign/design/organic-design-system.md` (tokens + per-screen visual spec) + `design/reference-logic.md` (parser/expiry/stepper/placement/checkout pseudocode with worked examples). Once G is implemented + verified, the handoff folder is **deleted** — the spec carries everything.

- [x] **G0 — Spec `004` authored** (2026-07-13) — `spec.md` (`FR-UI-001..034`, `SC-UI-001..008`; US1 design system / US2 bottom nav / US3 smart quick-add / US4 inline inventory / US5 tap-to-place / US6 grocery checkout / US7 feedback+toast), self-contained `design/*`, `checklists/requirements.md` (gate PASS), `plan.md` (per-branch `impl/nextjs`) + `tasks.md` (T001–T028). ROADMAP Phase G entry + status-matrix row (impl/vite ☐ deferred).
- [x] **G1 — Tokens, fonts, shell** ✅ (`06c0a62`→shell commit) — `tailwind.config.ts` Organic ramps/fonts/radii/shadows; `src/index.css` tokens + base state rules; `next/font` Caprasimo/Figtree; cream page frame + brand header; added `lucide-react`; `app/nav.tsx` bottom tab bar (renamed labels, active `aria-current`, urgent badge); `src/lib/quick-parse.ts` (TDD). Nav test rewritten.
- [x] **G2 — Kitchen** ✅ — `ToastContext`/`PlacementContext` + global `Toast`; `QuickAdd` (live preview + staples) replaces `InventoryForm`; `UseSoonStrip`, `LocationFilter`, `QuantityStepper`; `InventoryList` rewritten (dot/expiry/stepper/delete, soonest-first, edit-in-place dropped); `InventoryPage` recomposed. Parser + inventory tests. **Note:** dropping the classic form also drops the EC-03 duplicate-merge *prompt UI* (server merge behaviour untouched).
- [x] **G3 — Recommendations + placement + calendar** ✅ — `MealCard`/`RecommendationsPanel` restyled + "Plan it"→`startPlacing`; `CalendarPage` rebuilt as the 7-day organic grid (today outlined, filled/empty slots, placement banner, `SuggestionsRail`); tap-to-place primary, DnD demoted. Calendar/MealCard/RecommendationsPanel tests updated.
- [x] **G4 — Groceries + Feedback + toast** ✅ — `GroceryListPage` rebuilt (progress, category groups, round-check rows, NL quick-add, inline "Done shopping" checkout replacing `CheckoutConfirmModal`); removed 5 old grocery components; feedback chat + history + completion card restyled onto tokens. Grocery/feedback tests updated.
- [x] **G5 — Verify** ✅ — lint clean (0 warn), **322 unit tests**, coverage **91.15%**, `next build` clean; **Playwright** E2E (`e2e/redesign.e2e.ts`) drives all 4 screens with **5 committed screenshots** (`e2e/screenshots/`); `validate-e2e.sh --no-agent` **9/9**; SC-UI-008 diff check ✓ (no `app/api/**`/`src/server/**` change); **`design_handoff_organic_redesign/` deleted** (spec self-contained). Fixed a quick-add expiry off-by-one (UTC-midnight anchor) caught via screenshot.

> **Deferred `impl/vite` implementation:** the shared spec `004` is inherited on `impl/vite` via sync, but no Vite-side redesign code is planned yet — a deliberate choice (revisit later), **not** an unexplained gap. The weekly drift-check should treat `impl/vite` Phase G as ☐-deferred.
>
> **Top risks:** (1) coverage dip from large view rewrites → co-locate tests + fully unit-test the pure parser; (2) font CLS/render-block → `next/font` self-host + `display:swap`; (3) accidental contract drift → keep the diff out of `app/api/**` + `src/server/**` (SC-UI-008 check); (4) Playwright is additive (new `e2e/` + script) — it does not replace Vitest or `validate-e2e.sh`.

## Carried-forward notes (don't lose these)

- **Constitution §1** still lists "Day, Week, Month, Year views" but only `/`, `/calendar`, `/grocery` routes exist. Pre-existing gap — triage in Phase B. (Shared concern; lives on `main` now.)
- **Test-dir drift (found in A2):** `packages/client/tests/` still has `pages/GroceryListPage.test.tsx` after source moved `pages/`→`views/`; test subfolders lag `src/components`. Triage in Phase B (on `impl/nextjs`).
- **plan.md Testing line** keeps "Vitest + RTL" — left as-is (still the test runner); optional: note `vitest.config.ts` decoupling.
- **holodeck image name (found in A3):** docker-compose.yml `holodeck` service uses image `ghcr.io/emtabiraobarias/fridge-planner:latest` — odd that the agent shares the app's image repo name. Glance during Phase B.
- **README cascade table** step 6 still points at `.specify/memory/constitution.md` (stale — **root `constitution.md` is canonical**). Separate doc cleanup, not migration scope.
- **`.env.local` undocumented (found in A4):** docker-compose.yml references `.env.local` as an optional `env_file` for the holodeck service, but it's not in `.env.example` or documented in the README. Document or remove — triage in Phase B.
- **CLAUDE.md §11 cascade** still points at `.specify/memory/constitution.md` for the spec-tweak cascade; root `constitution.md` is canonical. Pre-existing drift (per-branch CLAUDE.md, so fix on each `impl/*`).
- **ROADMAP duplication (resolved 2026-06-08):** both impl branches now carry a copy synced from `main`. **This `main` copy is canonical** — update it here; impl copies will lag until their next `git merge main` (acceptable for a working log). Don't hand-edit the impl copies.

## Session log (newest first)

| Date | Phase/Task | What changed | Next |
|------|-----------|--------------|------|
| 2026-07-13 | **Phase G impl — G1–G5 (impl/nextjs), COMPLETE** | **Built the Organic UI redesign end-to-end.** G1 tokens/fonts/cream shell + bottom tab bar (`lucide-react`, `next/font`, `quick-parse` TDD); G2 Kitchen (smart quick-add replacing the form, use-soon strip, stepper list, toast); G3 recs restyle + tap-to-place calendar (placement banner + suggestions rail, DnD demoted); G4 grocery inline checkout (removed `CheckoutConfirmModal` + 4 old grocery components) + feedback chat restyle; G5 verify. **Frontend-only — 0 changes under `app/api/**`/`src/server/**` (SC-UI-008 ✓).** 322 unit tests green, coverage 91.15%, lint 0-warn, `next build` clean, `validate-e2e --no-agent` 9/9, **Playwright `e2e/redesign.e2e.ts` 4/4 + 5 screenshots**. Fixed a quick-add expiry off-by-one (UTC anchor). Deleted the handoff folder. | (optional) promote spec `004` to `main`; impl/vite redesign (deferred) |
| 2026-07-13 | **Phase G kick-off (spec-first) — spec `004` on `impl/nextjs`** | Distilled the external design handoff into a **self-contained** organic-redesign spec: `specs/004-organic-redesign/spec.md` (`FR-UI-001..034`: Organic tokens/shell, bottom tab bar, smart quick-add, inline inventory + stepper, tap-to-place calendar, grocery inline checkout, feedback restyle + global toast; `SC-UI-001..008`; frontend-only, no API/contract change), `design/organic-design-system.md` (tokens + per-screen visual spec) + `design/reference-logic.md` (parser/expiry/stepper/placement/checkout pseudocode + worked examples), `plan.md` (per-branch) + `tasks.md` (T001–T028) + `checklists/requirements.md` (PASS). Added Phase G to at-a-glance + status matrix (**impl/vite ☐ DEFERRED by decision**) + a Phase G section. Handoff folder to be deleted after G5. | Implement G1–G5 → Playwright screenshots → delete handoff |
| 2026-07-11 | **Phase F impl — F1–F5 (impl/nextjs)** | **Built the feedback collector end-to-end.** Per-branch `plan.md`/`tasks.md` (`f74c5f5`); second Holodeck agent `agents/feedback-collector/` (`:8002`, raw-JSON protocol, no web tools) + compose + `FEEDBACK_AGENT_URL` (`600e2a0`); backend model/service/controller/export + `/api/v1/feedback/**` (10/min chat, agent-fail→502 draft-preserved, ~30-turn finalize); `/feedback` chat + review UI (`8e8dd3f`); CLAUDE.md §9a (`ceb2a5f`). **323 tests green, coverage 91.87%, `next build` clean, `validate-e2e --no-agent` 9/9, live agent smoke green** (collecting / FINALIZE-complete / injection-resisted). ⚠ Fixed 2 base-image drifts (`d0952f7`): `holodeck-base:latest` rejects `setting_sources` + lacks Node.js. | F6 prod second-agent container (deferred); optional impl/vite build (deferred) |
| 2026-07-11 | **Phase F kick-off (spec-first) — spec `003` on `main`** | Drafted the shared, topology-agnostic **feedback-collector** spec `specs/003-feedback-agent/spec.md` (`FR-F-001..012`: conversational collection, draft-from-first-message persistence, structured spec-shaped record, per-user isolation → 404, spec-template markdown export, ~30-turn cap + finalize, 10/min chat rate limit, raw-JSON replies, prompt-injection resistance; `SC-F-001..005`; US1 chat / US2 review+export / US3 resume-discard) + `checklists/requirements.md` (all pass) + shared `checklists/acceptance-scenarios.md`. Added the **Phase F** section, at-a-glance entry, and status-matrix row (**impl/vite ☐ DEFERRED by decision**; impl/nextjs ◐). Next: merge `003-feedback-agent`→`main`, `sync-impls.sh`, then `/speckit.plan`→`/speckit.tasks`→`/speckit.analyze` on `impl/nextjs`. | plan/tasks/analyze on `impl/nextjs` → implement (F2–F5) |
| 2026-07-05 | **Phase E — E0b + Stage-1 deploy (impl/nextjs)** | **🎉 Stage-1 INTERNAL deployment COMPLETE + verified.** Finished E0b (OIDC authorization-code + PKCE callback; `3178684`,`e3463ae`) and stood the full stack up on a Portainer/TrueNAS node at `https://fridgeplanner.lan:8443` (Caddy + Next + Mongo + Holodeck + Keycloak/Postgres). `s8int-smoke` green: trusted TLS, OIDC login round-trip, live AI recs, data persists on restart, no leaked ports, no `AUTH_ALLOW_DEV`. 3 CI-built images; 6 bring-up fixes (server.js path, agent.serve.yaml, baked Caddyfile, 8080/8443 remap, token race, ```json fence). | Stage 2 (public) — deferred by choice |
| 2026-06-29 | **Phase E — E1 (impl/nextjs)** | **CI gate DONE** (`0193e91`): `.github/workflows/ci-nextjs.yml` — push/PR to `impl/nextjs` → `npm ci` → lint → test → `validate-e2e.sh --no-agent` (Node 20; e2e brings up Mongo via docker compose, no service-container clash; no LLM creds). Verified locally: lint OK · 283 tests · e2e 9/9. Closes the "no CI" TODO; branch protection on the `verify` check is a manual repo-settings remainder. | E2 (gated CD) ← needs E3 infra |
| 2026-06-29 | **Phase E — E0a (impl/nextjs)** | **Auth client wiring DONE** (`216199d`): closes the oidc-prod blocker — `apiFetch` Bearer injection in `src/services/http.ts` (all 15 calls), `AuthContext` (token + `NEXT_PUBLIC_OIDC_*` login redirect) mounted in `app/providers.tsx`, AuthBanner Sign-in button. +9 tests (283 total green), lint/build clean, coverage 93%. E0b (callback PKCE exchange) deferred → E3 (needs live IdP). | E1 (CI workflow) |
| 2026-06-27 | Phase E planned | Added **Phase E — Production rollout & gated CI/CD** (`impl/nextjs`-only; deploy via `nextjs-v*` tag, no merge to main): tasks E0–E7 + at-a-glance + NEXT ACTION. Detailed prereqs/runbook in per-branch `docs/deployment.md`. E0 (auth client wiring) flagged as the blocker. | E0 → CI/CD scaffolding |
| 2026-06-27 | weekly drift-check | **GREEN** branch-sync (12 shared files byte-identical across all 3 branches; sync-lag 0/0) · **YELLOW** content (0 CRITICAL/HIGH, 2 MEDIUM, both fixed): added shared `AUTH-*` acceptance scenarios + refreshed stale ROADMAP (LAST-LEFT-OFF / D1). | (no phase queued) |
| 2026-06-27 | **Phase D impl — impl/vite (COMPLETE both)** | OIDC auth on Express (`40414d2`): `authMiddleware` verify + dev seam (single `/api/v1` seam), `errorHandler`→401, SPA `AuthBanner` 401 UX. +20 tests; server 199→216, client 114→117; tsc build clean; **E2E gate 9/9**. **🎉 Phase D done on both impls.** | (no phase queued) |
| 2026-06-27 | **Phase D impl — impl/nextjs** | OIDC auth implemented (`3d0a3bb`+`69a415e`): `authenticate()` (jose verify + dev seam + prod guard), `withRoute`→401, 12 handlers, public `/api/health`, client `AuthBanner` 401 UX. +16 tests; client 258→277; lint/build clean; **E2E gate 9/9**. Auth-mode seam: `validate-e2e` boots prod build with `AUTH_MODE=dev`+`AUTH_ALLOW_DEV=true` (two-flag ack; FR-D-008 intact). tasks.md complete. | implement impl/vite (mirror) |
| 2026-06-27 | Phase D analyze (both impls) | `/speckit.analyze` (spec↔plan↔tasks) on both branches (`c069e03` nextjs, `c1ccb63` vite). Caught **A1: FR-D-009 (client 401→re-auth) had no task** → added frontend task + plan step both impls; A2 tightened cross-user 404 to all resource types; A3 added clock-skew/JWKS-rotation edges; A4 SC-D-003 design-satisfied. | TDD implement (nextjs→vite) |
| 2026-06-27 | Phase D tasks (both impls) | Drafted per-branch `tasks.md`: impl/nextjs `3bced73` (T001–T012, 12-handler swap) + impl/vite `8387923` (T001–T011, single-middleware seam). TDD-ordered, FR-D traceability, `[P]` markers. Per-branch files. | /speckit.analyze → implement |
| 2026-06-27 | Phase D plans (both impls) | Drafted per-branch `specs/002-authentication/plan.md`: **impl/nextjs** `3d33d58` (verify in Next server layer — `authenticate()`/`jose`/`withRoute`→401) + **impl/vite** `f208bdd` (rewrite single Express `authMiddleware`/`jose`/`errorHandler`→401). Same contract/lib/env/dev-seam; only the insertion point differs. Per-branch files (not on main). | tasks.md → analyze → implement |
| 2026-06-27 | **Phase D kick-off (spec-first)** | Drafted shared topology-agnostic auth spec `specs/002-authentication/spec.md` on `main` (`FR-D-001..009`: OIDC/JWT validation, identity from `sub`, FR-036 isolation, 401/404 Problem JSON, dev/test seam) — elevates `001` CR-001/CR-002/FR-036/Assumption 12. Added a Phase D section + status-matrix/at-a-glance updates. Both impls inherit; enforcement per-branch (Express middleware vs Next server layer). | per-impl plan → tasks → implement |
| 2026-06-27 | **C-bis Cb6 — COMPLETE** | Deleted `packages/server` (`70af3b9`; gate passed) → single source of truth; root `package.json` client-only workspace + `concurrently` dropped + **v4.0.0**; doc cascade `e926e68` (CLAUDE.md/plan.md/DEVELOPMENT.md → Next Route Handlers) + README `1414269`. Client 258/258, lint clean, build + E2E green. **🎉 Phase C-bis done.** | impl→main merge decision / Phase D |
| 2026-06-27 | E2E validation infra | **Reusable E2E smoke gate.** Shared `scripts/smoke-test.sh` (steps + `--no-agent`; `a200439`, SMOKE_USER fix `b0d6940`) synced to both impls; per-branch `scripts/validate-e2e.sh` boot wrappers — impl/nextjs `110dc1b` (:3000) + impl/vite `3a735ef` (:3001); `docs/smoke-test.md` release-gate guidance. Both gates 9/9 green. Gate caught + fixed a latent vite Express `tsc` build break (`6258157`). | Cb6 |
| 2026-06-26 | C-bis real-boot + agent test | **Real-boot smoke PASSED** (`next dev`/`start` vs live Mongo: 9/9 API + UI walkthrough incl. live agent 5 real meals + consumption 3→2 + grocery gen; `next build` ok). Added live agent-client test (mocked fetch; `7d87711`). Closed the `packages/server`-deletion gate. | E2E infra → Cb6 |
| 2026-06-25 | C-bis Cb5 | **retired Express from runtime** (soft-retire): ported middleware to `src/server` (in-memory rate limiter 10/min→429 + `withRoute` Problem-JSON error wrapper on all 12 routes; `e95ee95`), stopped running Express (dev/build scripts + docker-compose + CORS/PORT/BACKEND_URL; `9d8c09b`), ported 88 pure-logic unit tests Jest→Vitest (`a2308d4`). `next build` validates handlers; `npm test` server 199 + client 252. `packages/server` kept as net. | real-boot smoke test → Cb6 |
| 2026-06-24 | C-bis Cb4 | migrated **recommendations** (last endpoint) → Next Route Handler over `controllers/recommendations.ts`; preserved EC-01/cache/EC-08 fallback ladder; copied meal-recommender + popular-recipes; **removed the next.config proxy entirely**; 5 node-env tests (mocked agent). Client 154→159, server untouched. ⚠ rate limiter deferred to Cb5. `f627c48` | Cb5 (retire Express) |
| 2026-06-24 | C-bis Cb3 | migrated **meal-plans** → Next Route Handlers (GET, `entries` POST, `entries/[slotId]` DELETE, PUT) over `controllers/meal-plans.ts`; preserved reversible consume/restore (BUG #7/FR-005); added framework-neutral `src/server/logger.ts` to decouple `ingredient-consumption` from Express's `app.ts`; 11 node-env tests drive real consume/restore. Client 143→154, server untouched. `e395890` | Cb4 (recommendations, last — Holodeck dep) |
| 2026-06-24 | C-bis Cb2 | migrated **grocery-lists** → Next Route Handlers (6 routes under `[weekStart]/`: GET lazy-gen, `generate`, `items` POST, `items/[itemId]` PATCH/DELETE, `complete`→inventory) over `controllers/grocery-lists.ts`; copied grocery libs + grocery-list/meal-plan models into `src/server`; 16 node-env handler tests. Client 127→143, server untouched. `71c5214` | Cb3 (meal-plans + logger decouple) |
| 2026-06-23 | C-bis Cb1 | migrated **inventory** → Next Route Handlers (`app/api/v1/inventory/{route,[id]/route}.ts`) over extracted controller + `http.ts`; copied model/expiration/cache/types into `src/server`; proxy → explicit allow-list; node-env handler tests vs `mongodb-memory-server` (`@server`+`server-only` aliased). Client 118→127, lint clean, server untouched. `fad36c7` | Cb2 (grocery-lists + checkout) |
| 2026-06-23 | C-bis Cb0 | scaffolded `src/server/db.ts` (globalThis-cached Mongoose, `server-only`); added `mongoose`/`zod`/`server-only` deps + `@server/*` alias. No behavior change. `e57e82a` | Cb1 (inventory) |
| 2026-06-23 | coverage gate | restored server 80% branch gate (latently red 78.41% from Phase C #6/#7) → 81.29%: unit tests for `expirationStatusQuery`/`notExpiredQuery` + a `restoreIngredients` suite (incl. catch-block error paths). Test-only. Lead `impl/vite` `d475dc4` → cherry-pick `impl/nextjs` `4f8132e`. Server 185→199 | (unblocks C-bis npm test) |
| 2026-06-08 | strategy step 3 (sync) | genericized constitution backend (Express vs Route Handlers→per-branch); merged `main`→both impl branches (spec tightening propagated to `impl/vite`); added "Stack Realization" to both plan.md; reconciled disjoint `origin/main` (LICENSE/README) via `--allow-unrelated-histories` | Phase B verify on `impl/nextjs` |
| 2026-06-08 | strategy steps 1–2 | verified §6 topology; landed shared spec+checklists+genericized constitution (3.1.0)+strategy+roadmap on `main`; renamed branches→`impl/vite`+`impl/nextjs` (local+origin); reconciled CLAUDE.md §8 on `impl/nextjs` | step 3 sync discipline |
| 2026-06-07 | branching strategy (design) | designed two-impl model; wrote specs/BRANCHING_STRATEGY.md handoff; linked in CLAUDE.md | Claude Code executes |
| 2026-06-07 | A5 | committed Phase A docs + analyze'd spec on branch, tagged `migration-docs-reconciled` (not merged) | Phase B (verify claims) |
| 2026-06-07 | analyze | `/speckit.analyze` run; spec.md tightened (FR-013 removed, FR-034/35 deferred, FR-003/028 clarified) | A5 commit |
| 2026-06-07 | A4 | swept 5173/nginx/vite (clean); added CORS_ORIGIN to .env.example | A5 (commit, no merge) |
| 2026-06-07 | A3 + skill fix | README.md → Next.js (verified vs disk); moved skill to `.claude/skills/weekly-drift-check/` | A4 (.env.example sweep + analyze) |
| 2026-06-07 | A2 | plan.md → Next.js; source tree verified vs disk | A3 (README.md) |
| 2026-05-31 | A1 | constitution.md → Next.js + v3.0.0 | Commit on branch; then A2 (plan.md) |
