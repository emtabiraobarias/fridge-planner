# Quickstart — Daily Rolling Grocery-List Refresh (`impl/nextjs`)

Dev/test walkthrough for spec 008. Prereqs: MongoDB and Holodeck available for normal app flows (`docker compose up -d mongodb holodeck`), plus `packages/client/.env.local` with `MONGODB_URI`, `HOLODECK_URL`, and `AUTH_MODE=dev` for local manual testing.

> Spec 008 is a superset of 006/007: cook shrinks needs (006), buy grows inventory (007), **day rollover sheds stale meals + rows (008)**. "Today" is derived from the server clock — to exercise rollovers manually, change the host date/clock (or use fake timers in tests) rather than any UI control.

## Run it

```bash
npm run dev
```

1. **Date-scoped generation (RG1)**: Calendar → plan a planned (uncooked) dinner for **yesterday** and one for **tomorrow**, each needing the same ingredient → Grocery → Regenerate. Only the tomorrow dinner's ingredients appear; the yesterday dinner contributes nothing (FR-RG-001). A line sourced from both shows only the tomorrow meal's shortfall and lists only the tomorrow meal in its sources (FR-RG-007).
2. **Removal of fully-past needs (RG1)**: with a generated, unpurchased line whose only source meal was yesterday, Regenerate → the line is gone (FR-RG-006).
3. **Same-day integrity (RG2)**: tick a generated line purchased (Kitchen increments per spec 007; the row now carries `purchasedOn`=today) and add a manual item (`addedOn`=today). Refresh the grocery page repeatedly the same day → both rows persist unchanged, receipt intact (FR-RG-004/005). Un-tick the purchased row → it reverses exactly per its receipt (FR-RG-011), unaffected by the refreshes.
4. **Daily shed (RG2)**: advance the host clock past midnight (or set the meal/purchase to "yesterday"), refresh → the purchased row and the manual item are gone from the list; Kitchen still holds the purchased stock and the list does **not** re-ask for it (owned stock nets it off — FR-RG-005/011). Un-ticking the shed row is no longer offered (404).
5. **Auto-freshness on view (RG3)**: with a list last computed "yesterday," open the Grocery page today with no explicit action → the shown needs already reflect today's scope (FR-RG-002, SC-RG-002). Cook a today-onwards meal, reopen → its need is reduced with no manual regenerate (SC-RG-004). Manual Regenerate produces an identical list (FR-RG-002 scenario 2).
6. **Future-week view (RG3)**: navigate to next week's list → all of that week's planned meals count, since every date is today-or-later (FR-RG-002 scenario 3).

## Test it

```bash
npm -w packages/client run test -- tests/server/unit/rolling-grocery.test.ts
npm -w packages/client run test -- tests/server/grocery-lists.test.ts
npm test
npm -w packages/client run build
npm -w packages/client run test:e2e
bash scripts/validate-e2e.sh --no-agent
```

- Use `vi.useFakeTimers()` + `vi.setSystemTime()` to cross the midnight boundary deterministically; add a focused boundary case at local 23:59 vs 00:01 to lock down the local-vs-UTC cutoff (Research D3).
- Server tests seed meal-plan entries with explicit `date` values relative to a fixed "now," assert generated needs before/after the cutoff, and fire a recompute between a tick and an un-tick to prove receipt preservation (FR-RG-011).

## Verification log

### T001 — Baseline (before RG1), 2026-07-22
- `npm run lint` → clean (0 warnings/errors).
- `npm test` → **55 test files passed, 546 tests passed**, 0 failures (~duration 12s wall). Coverage gate met. (Some passing tests print expected error stack traces — no failures.)

### T002 — Existing grocery-list assertions that assume 007 "generate once, then verbatim"
Reviewed `packages/client/tests/server/grocery-lists.test.ts`. The following assume 007 semantics and are consciously reshaped by US1/US3 **later** (NOT modified in Phases 1–3):
- `GET › 'lazily generates a list from the meal plan on first GET'` (L146–153): asserts `items.length > 0` from a **past-dated** seed (`validMealEntry.date = 2026-04-06`). This relies on GET's lazy-create NOT being date-scoped. Preserved in Phases 1–3 because getGroceryList keeps the 007 path (generator called without `asOf`). US3 (T022/T025, not mine) recomputes GET on-view and will re-anchor this to explicit dates.
- `GET › 'aggregates the same ingredient across multiple meals'` (L155–161): same past-dated seed; relies on GET returning generated content without date scope. US3 will re-scope.
- `GET › 'isolates lists by user'` / `'returns { groceryList: null }'`: null/isolation paths — unaffected by rolling (US3 keeps the null short-circuit, T023/T026).
- `POST generate › 'regenerates and preserves manually-added items'` (L181–189): seeds past-dated `validMealEntry` + a manual "Bread". Under US1 (T013) the past meal contributes nothing, but the manual row is sticky pass-through, so the sole assertion (`bread.isManuallyAdded === true`) still holds. Not modified.

Reconfirmed there is **no** existing assertion that GET returns a stored document byte-verbatim on a second read, so US3's recompute-on-view has no direct 007 test to overturn — only the past-dated lazy-generate expectations above.

### RG-phase per-task log (Phases 1–3, T001–T013)

- **T003/T004/T005** — Added `addedOn?`/`purchasedOn?` to server `IGroceryListItem`, client `GroceryListItem` (ISO string), and the Mongoose `groceryListItemSchema` (`type: Date, required: false`, no default). Optional-only, no migration.
- **T006 (TDD)** — Wrote `startOfTodayCutoff` unit tests first; observed failure: `Failed to load url @server/lib/rolling-grocery … Does the file exist?` (module absent). After T007 the 3 tests pass, including the 23:59-vs-00:01 boundary under `TZ=Australia/Sydney` (research D3). **Runner note:** vitest must be run with cwd `packages/client` (`vitest run --coverage` is configured there); running from repo root leaves the `@server` alias unresolved.
- **T007** — Created `src/server/lib/rolling-grocery.ts` with real `startOfTodayCutoff()` + a throwing `reconcileRollingList` stub. T006 → green.
- **T008 (TDD)** — Reconcile unit tests written against the stub; observed failure: 5 tests throw `reconcileRollingList not implemented (Phase 3 / US1)` (3 startOfTodayCutoff still green).
- **T009 (TDD)** — Extended `grocery-list-generator.test.ts` with date-scope cases; observed failure: `expected 2 to be 1` (soy sauce counted both meals) and `expected 3 to be 2` (servings fallback counted the past meal). The cooked-yesterday, since-shed-netting, and asOf-omitted cases passed pre-impl (already covered by status/netting).
- **T010 (TDD)** — Added `POST /generate` rolling-scope handler tests; observed failure: `expected 2 to be 1` (all-week count) and `_id` mismatch (old regenerate wiped+recreated generated rows). The FR-RG-006 drop case passed pre-impl (non-manual rows were already dropped by 007 concat).
- **T011** — `generateGroceryList` gained optional `asOf?: Date`; filters `planned` entries to `entry.date >= asOf` when supplied. **Deviation (intentional):** `asOf` is optional, not required — this keeps `getGroceryList`'s lazy-create path on the exact 007 semantics (called without `asOf`) as the US1 checkpoint requires ("GET still uses the old 007 path until Phase 5"), and preserves the existing past-dated GET tests. `regenerateGroceryList` always passes the cutoff.
- **T012** — Implemented `reconcileRollingList(existing, freshGenerated, _asOf)`: partitions by `isReplaceableGenerated` (`!manual && !purchased && !receipt`), diffs replaceable rows by `ingredientName` (keep `_id`, overwrite `quantity`/`unit`/`sourceMealNames`; drop on zero/absent; insert new), sticky rows pass through verbatim. `_asOf` reserved for the US2 shed (lint-clean via `_` prefix).
- **T013** — `regenerateGroceryList` now computes `asOf = startOfTodayCutoff()`, generates date-scoped, and merges via `reconcileRollingList(existing.toObject().items, generatedItems, asOf)` (`.toObject()` avoids the 006 hydrated-subdoc spread bug). `getGroceryList` untouched.
- **Phase 1–3 result** — Focused `rolling-grocery.test.ts` + `grocery-list-generator.test.ts` + `grocery-lists.test.ts` = **57 passed**. `npm run lint` clean. Full `npm test` (with coverage) = **56 files / 562 tests passed** (baseline 546 → +16). `npm -w packages/client run build` clean.

### Phase 4 per-task log (US2, T014–T021), 2026-07-22

- **T014 (TDD)** — Extended `rolling-grocery.test.ts` with a new `reconcileRollingList — sticky rows` describe (6 cases: manual preserved-today, manual shed, purchased preserved-today, purchased shed, legacy-manual backfill, legacy-purchased backfill). Observed failure: **4 of 6 failed** pre-implementation (the two "preserved today" cases already passed — sticky rows were unconditional pass-through since T012 — but both shed cases failed because rows were never dropped, and both backfill cases failed because `addedOn`/`purchasedOn` stayed `undefined`).
- **T015 (TDD)** — Extended `grocery-lists.test.ts` PATCH/complete coverage: tick-stamps-`purchasedOn`, regenerate-after-tick keeps the receipt, mid-shop-race recompute-then-untick, shed-row untick → 404 (not 409), and checkout stamps `purchasedOn`. Observed failure: **all 5 new assertions failed** (`purchasedOn` undefined; shed-row test found the row still present so the 404 assertion failed with the row object instead).
- **T016 (TDD)** — Extended `grocery-lists.test.ts` `addGroceryItem` coverage: `addedOn` stamped on add, survives a same-day regenerate. Observed failure: both failed (`addedOn` undefined).
- **T017 (TDD)** — Added the concurrency-shaped test. **Deviation (intentional, per orchestrator instruction "GET still has 007 semantics — do not touch `getGroceryList`")**: the task text says "GET the list (recompute)…", but at this phase GET has not yet been wired onto the shared reconcile path (that's US3/T025). `POST .../generate` already runs the exact same `reconcileRollingList` mechanism GET will reuse in US3 (research D2 — "one date-scoped generation path shared by lazy GET and force-generate"), so the test recomputes via regenerate and then ticks the row it returns. T029 (Phase 6) is reserved for the literal GET-based variant once US3 lands. Observed failure: `purchasedOn` undefined on the ticked row (the id-stability half already passed, since T012's reconcile-by-name was already in place).
- **T018** — `addGroceryItem` now stamps `addedOn` on the new manual item.
- **T019** — `purchaseGroceryItem`'s guarded first write now stamps `purchasedOn` in the same `$set` that flips `isPurchased: true` (not the later receipt-write). `completeGroceryList`'s `processCheckoutItem` stamps `purchasedOn` in the same write that creates a new receipt at checkout. (The pre-existing defensive branch — receipt already present but `isPurchased` false — is left unstamped: that row's true purchase anchor was already set by the original tick and restamping it to "now" at checkout would incorrectly extend its shed window.)
- **Mid-implementation correctness finding (T018/T019 deviation from literal task wording)**: implementing `addedOn`/`purchasedOn` as raw `new Date()` (as tasks.md/the orchestrator prompt literally say) broke 4 tests on this AEST (+10) host: `startOfTodayCutoff()` projects the *local* calendar day onto a UTC-midnight instant, which — for any positive-UTC-offset timezone — sits *ahead* of the real current UTC instant for the first `|offset|` hours of the local day (here, until ~10am AEST). A row added at 06:48 AEST got `addedOn = 2026-07-21T20:48Z` (real now) while the very next regenerate computed `asOf = 2026-07-22T00:00Z`, so `addedOn < asOf` → the row shed itself within hours of being created. The API contract's own example payload (`"purchasedOn": "2026-07-22T00:00:00.000Z"`, exact midnight, not a wall-clock timestamp) confirms the anchor is meant to live on the same projected axis as `asOf`, not real time. **Fix**: `addedOn`/`purchasedOn` are stamped with `startOfTodayCutoff()` (imported from `rolling-grocery.ts`, already in scope in the controller), not `new Date()`. Test assertions were adjusted to compare against `startOfTodayCutoff().toISOString()` instead of a real-time "before" bound (which had the identical flaw). This is a **correctness fix**, not a scope change — the FR-RG-004/005 stamping behavior and the contract's response shape are unaffected; only the literal expression differs from the tasks.md shorthand.
- **T020** — `reconcileRollingList` gained a `reconcileSticky(item, asOf)` helper: anchor = `purchasedOn ?? addedOn`; `undefined` anchor → lazy-backfill (`purchasedOn` if `isPurchased || purchaseReceipt`, else `addedOn`) and keep for this call; anchor `< asOf` → shed (return `undefined`, row dropped); otherwise preserved verbatim. The `_asOf` parameter (previously unused, US1-reserved) is now `asOf` and used.
- **T021** — Confirmed (no structural change needed) that `patchGroceryItem`'s `findOne({..., 'items._id': objectId})` guard already runs *before* `unpurchaseGroceryItem` is called, so a shed row (absent from the array once a recompute has pruned it) already 404s ahead of `unpurchaseGroceryItem`'s 409 checks — verified live by the T015 shed/untick test. Added `purchasedOn` to the `$unset` alongside `purchaseReceipt` on a successful un-tick.
- **Phase 4 result** — Focused `npx vitest run --coverage=false tests/server/unit/rolling-grocery.test.ts tests/server/grocery-lists.test.ts tests/server/unit/grocery-list-generator.test.ts` (run from `packages/client`) = **71 passed** (14 + 35 + 22). `npm run lint` clean.

### Phase 5 per-task log (US3, T022–T027), 2026-07-22

- **T022 (TDD)** — Added a new `GET grocery-lists/[weekStart] — rolling recompute-on-view (spec 008 US3)` describe with 5 cases: stale-doc recompute (FR-RG-002 scenario 1), GET-then-generate byte-identical `items` (scenario 2), future-week counts all planned meals (scenario 3), fully-past-week recomputes to empty `items` (FR-RG-009), and the unchanged null path (duplicated here as an explicit US3 guard, see T023). Observed failure: **2 of 5 failed** pre-implementation — the stale-doc case (`existing` was returned verbatim: `nori` stayed present) and the fully-past-week case (old GET's lazy-only path called `generateGroceryList(mealPlan, inventory)` with **no** `asOf`, so it counted both past entries and returned 2 items instead of `[]`). The scenario-2/scenario-3/null-path cases passed pre-impl by coincidence (their entries are all future-dated, so the old ungated GET generator and the new date-scoped one agree) — these still serve as real regression guards post-implementation, noted rather than treated as red herrings.
- **T023** — Folded into the T022 describe block as `'still returns { groceryList: null } when no meal plan and no stored list exist for the week (unchanged null path)'` — passed pre-impl (007 behavior already correct here); kept as an explicit US3-scoped guard per the contract's "Unchanged: ... still returns `{ groceryList: null }`" note, since T025/T026 could regress it if the short-circuit were dropped.
- **T024** — Added `GroceryListContext.test.tsx` case asserting `refresh()` (and the initial mount fetch) surface whatever `fetchGroceryList` resolves with, verbatim, across two different mocked responses — proving the context does no client-side date/scope filtering of its own. Also added a `Refresh` button to `TestConsumer` (context already exposed `refresh`, just untested). This test **passed immediately** (the context was already a thin server-trusting pass-through pre-008) — logged as a regression guard, not a red-to-green case; the context required no code change for US3.
- **T025/T026** — Extracted a shared `recomputeRollingList(userId, weekStart, mealPlan, existing)` helper in `grocery-lists.ts` (D2: one date-scoped path for GET *and* force-generate) that computes `asOf = startOfTodayCutoff()`, generates date-scoped needs (`mealPlan ? generateGroceryList(mealPlan, inventory, asOf) : { items: [], generatedAt: new Date() }` — a cleared/never-had meal plan is treated as zero in-scope entries, not an error, so a still-sticky document keeps reconciling), reconciles via `reconcileRollingList`, and persists via the existing `findOneAndUpdate(..., { upsert: true, new: true })`. `getGroceryList` now fetches `existing` and `mealPlan` in parallel, short-circuits to `{ groceryList: null }` only when **both** are absent (T026), and otherwise calls the shared helper unconditionally — no more "only when absent" gate. `regenerateGroceryList` now calls the same helper (after its existing 404-when-no-plan guard), replacing its bespoke inline generate+merge block — this is what makes T022 scenario 2 (GET/generate byte-identical) hold structurally, not just by test luck. Cyclomatic complexity of both handlers stayed low (helper carries the branching); lint clean.
- **Existing-test reshape (per T002 note, not a new task but required to keep the suite green)** — Two pre-008 `GET grocery-lists/[weekStart]` tests assumed 007's generate-once-then-verbatim semantics via the shared past-dated `validMealEntry` (fixed `2026-04-06`, now in the past): `'lazily generates a list from the meal plan on first GET'` and `'aggregates the same ingredient across multiple meals'`. Under T025 both now correctly return an empty/undefined result for that fixed past date (rolling scope), so both were re-anchored to seed entries with `utcMidnightOffset(0)`/`utcMidnightOffset(1)` (in-scope dates) via a spread override on the same `validMealEntry`/`secondMealEntry` fixtures, renamed to `'generates a list from the meal plan on GET when the entry is in scope'` and `'aggregates the same ingredient across multiple in-scope meals'`. This is the contract-correct re-anchor flagged in T002 — not a weakening: the assertions (items present; soy sauce quantity 2) are unchanged, only the seed dates moved from "accidentally past" to "deliberately in scope." The `POST generate › 'regenerates and preserves manually-added items'` test T002 also flagged needed **no** change — confirmed still green as predicted (manual row is sticky pass-through regardless of the past-dated meal).
- **T027** — Reviewed `GroceryListPage.tsx` and `GroceryListContext.tsx`. Found one misleading line: the empty-state copy `"Plan meals for this week and tap Regenerate, or add something below."` implied Regenerate was required to see fresh needs; since GET now recomputes on every view, reworded to `"Plan meals for this week — your list updates automatically, or add something below."` No other changes: the `Regenerate` button/label and `generate()`/`generateGroceryList` naming were left as-is — force-regenerate remains a legitimate distinct action (an explicit same-session resync, e.g. after editing the meal plan without navigating away), so the wording there wasn't misleading. No new interactive surface added.
- **Phase 5 result** — Focused `npx vitest run --coverage=false tests/server/grocery-lists.test.ts tests/context/GroceryListContext.test.tsx` (run from `packages/client`) = **50 passed** (40 + 10). `npm run lint` clean. Full `npm test` (with coverage) = **56 files / 582 tests passed**. `npm -w packages/client run build` clean (no route/type changes).

## Release handoff

- [ ] Create release/version tag after review
- [ ] Build and push deployment images
- [ ] Redeploy through Portainer and verify production health checks
- [ ] Run post-deploy smoke validation against the deployed URL
- [ ] Confirm spec 001 FR-025/026/030/031 cascade merged on `main` (FR-RG-012)

## Gotchas

- **GET now mutates**: recompute-on-view persists the reconciled document. Generated rows keep their `_id` via name-reconcile, so an in-flight tick from a prior render still targets a live row; a lost race already refetches on 404/409 (existing `GroceryListContext`).
- **Local vs UTC midnight**: meal entries are stored at UTC midnight (`getWeekDays`), the host runs local — the cutoff maps the local calendar day onto the UTC-midnight axis. Get this wrong and meals flip in/out of scope near midnight.
- **Shed is final for reversal**: after a purchased row sheds, its receipt is gone and un-tick 404s; the inventory addition stays. Correct a post-shed purchase via normal Kitchen editing, not un-tick.
- **Rolling recompute touches no inventory** — so it never invalidates the recommendation cache (only 007 tick/checkout do).
- **Legacy rows**: pre-008 manual/purchased rows without an anchor are lazily stamped to the recompute day, so they survive one day rather than vanishing on deploy.
- Do not add a scheduler/cron — recompute-on-view is the clarified mechanism (FR-RG-002).
