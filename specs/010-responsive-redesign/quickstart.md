# Quickstart — "The Fridge" Responsive Redesign (`impl/nextjs`)

Dev/test walkthrough for spec 010. Prereqs: MongoDB + Holodeck (`docker compose up -d mongodb holodeck`), plus `packages/client/.env.local` with `MONGODB_URI`, `HOLODECK_URL`, `AUTH_MODE=dev`. `OPENAI_API_KEY` drives the agent; at least one of `BRAVE_SEARCH_API_KEY` / `SPOONACULAR_API_KEY` gives usable recipe links (FR-037). None of the AI keys are needed to exercise US1–US4 or US6 — only the Home banner CTA (US5) and the suggestions rails call the agent, and both are explicit-tap-only.

> Spec 010 is **presentation only**: no endpoint, no schema, no route rename (FR-RS-026). If any shipped flow behaves differently after this work — inventory edit/delete/expiry/select-mode, drag-and-drop on large viewports, cook/un-cook, immediate grocery purchase and reversal, rolling list scope, feedback chat/promote/pipeline — that is a **regression**, not a feature (SC-RS-007). The canonical values for every breakpoint, padding, colour, radius and copy string are in [`design/responsive-system.md`](design/responsive-system.md); `spec.md`'s *Alignment reconciliation* table is binding wherever the design and shipped behaviour disagree.

## Run it

```bash
npm run dev          # http://localhost:3000
```

### Exercising the five viewport classes locally

Use Chrome DevTools device toolbar (`⌘⇧M`) and set the viewport **by size, not by device preset** — FR-RS-001 forbids device sniffing, so only the dimensions matter:

| Viewport class | Set this size | What must be true (design §1.1/§1.3) |
| --- | --- | --- |
| Phone portrait | **390 × 844** | Bottom-centre floating **pill** nav; content padding top 54 / sides 16 / bottom 116; feedback **bubble** 56×56 at right 16 / bottom 96 |
| Phone landscape | **844 × 390** | Left-docked vertical **rail**, vertically centred; padding top 30 / **left 96** / right 16 / bottom 44; bubble 54×54 at right 16 / bottom 20 |
| iPad portrait | **820 × 1180** | Pill nav; padding top 40 / sides 34 / bottom 116; bubble 60×60 at right 26 / bottom 100 |
| iPad landscape | **1180 × 820** | Left **rail**; padding top 40 / **left 120** / right 34 / bottom 44; bubble 60×60 at right 24 / bottom 24 |
| Desktop | **1440 × 900** | Persistent left **sidebar** (250px expanded / 76px collapsed); padding top 32 / sides 40 / bottom 48; content capped at 1120px centred; feedback **`Tell us` pill** 54px high at right 32 / bottom 32 |

> **The 844 × 390 check is the important one.** Phone landscape is the only viewport class Tailwind cannot express with a stock breakpoint (it is width-*and*-orientation-*and*-height bound, research D1). At 844 wide, `sm:` (≥640) also matches, so the custom `phland` screen must win the cascade. Verify by inspecting the padded content wrapper: computed `padding-left` must be **96px**, not 34px. If it reads 34px, `phland` is not declared last in `tailwind.config.ts` → `extend.screens`.

### Per-story verification

**US1 — the shell and navigation (FR-RS-001..007)**

1. At each of the five sizes above, open all five screens (`/home`, `/`, `/calendar`, `/grocery`, `/feedback`). Navigation is present and in the mode the table says; the feedback affordance is present and never overlaps the nav (FR-RS-002/006).
2. **No horizontal page scroll anywhere** (SC-RS-001). Check in the console rather than by eye:
   `document.documentElement.scrollWidth === document.documentElement.clientWidth` → `true`.
3. **Only the content region scrolls** (FR-RS-004, SC-RS-002). Add enough inventory to overflow, then scroll: the nav and the feedback affordance must not move. Confirm `document.body` itself does not scroll — the scrollbar belongs to `<main>`.
4. **Wide content scrolls inside its own container** (FR-RS-007): at iPad landscape, the 7-day calendar grid scrolls horizontally *within its own box* while the page does not.
5. **Sidebar collapse persists** (FR-RS-003, SC-RS-009): at desktop, collapse the sidebar with the chevron (`aria-label="Toggle navigation"`), confirm items remain identifiable as icons, then **reload** — it is still collapsed, and it does not visibly animate closed on load. Check `localStorage.getItem('fp:nav:collapsed')`.
6. **Orientation change** from 390×844 to 844×390 while on any screen: nav switches pill → rail and content re-insets, with no horizontal overflow at either size.

**US2 — the fridge shelves (FR-RS-008..011)**

7. Seed items across all three locations, e.g. quick-add `2L milk expires friday`, `spinach exp 1d`, then use the edit sheet to move one item to `freezer` and one to `pantry`.
8. One shelf card per location — **Fridge / Freezer / Pantry** — each with its tint (design §4.2.3), its name, and an `N items` count; at the viewport's column count (1 / 2 / 3 per design §1.2). A location with no items shows a **zero count and an empty hint**, not nothing.
9. Step a quantity with `−`/`+`: it moves by that item's step, **floors at zero without deleting the row** (FR-RS-009 — see Gotchas: this is an intentional change from shipped behaviour), persists across reload, and a zero-quantity chip shows the neutral dot.
10. Status is distinguishable **by more than colour**: the expiring chip keeps its text expiry line alongside the dot (FR-RS-009).
11. Everything shipped still works (FR-RS-010): edit (expiry + location), delete, and `009` **select mode** → "Find recipes with selected".
12. Type free text in the add pill: the spec `005` **parse preview with correctable provenance chips** still appears beneath it (FR-RS-011). The pill is a visual change only.

**US3 — the calendar hybrid (FR-RS-012..015)**

13. At **390 × 844**: a seven-day strip with one day selected, that day's meals listed, a dot on days that contain meals, and — on an empty day — the prompt *"Nothing planned for this day yet…"* (FR-RS-012).
14. At **1180 × 820** and **1440 × 900**: the shipped 7×4 grid, and **drag a planned meal to another slot** — it moves and persists (FR-RS-013). Per-slot clearing still works.
15. Inspect the DOM at desktop: there must be **exactly one** calendar layout mounted — no hidden day strip (research D4). A duplicate would break `e2e/calendar-dnd.e2e.ts`.
16. Cooked vs planned meals are visually distinct, and cook / un-cook behave exactly as spec `006` shipped, including the grounded inventory-clamped review and the receipt-based un-cook (FR-RS-014).
17. On first load of `/calendar`, **zero** recommendation requests fire (Network tab: no `POST /recommendations`); suggestions appear only after the explicit CTA, in **both** layouts (FR-RS-015, SC-RS-005).

**US4 — grocery and checkout (FR-RS-016..019)**

18. With a partially-checked list: a **progress ring** shows checked-of-total alongside the figure, and category groups sit at the viewport's column count (FR-RS-016).
19. Tap a row → it toggles and **immediately** adds to the kitchen with a receipt; untick → **exact reversal**. The ambiguous-quantity `PurchasePromptSheet` still appears where it applies (FR-RS-017). Verify inventory changed on `/`.
20. The finish-shopping button's count equals the **receipt-less** rows — not the unchecked ones — and completing it adds/marks only those and **does not clear the list** (FR-RS-018). The design's "N = unchecked, then clear" model is rejected (reconciliation item 13).
21. The **week the list covers** is evident in the header/progress card, so a row dropping out at a day rollover reads as spec `008`'s rolling behaviour rather than a bug (FR-RS-019).

**US5 — Home (FR-RS-020..022)**

22. Open **`/home`** (net-new route; `/` is still the Kitchen — FR-RS-026 forbids renaming, research D6). Four figures: items needing use soon, meals planned this week, shopping progress, total items tracked. Cross-check each against `GET /api/v1/inventory`'s `summary` and the meal plan / grocery list (SC-RS-006).
23. **Zero AI requests on load** (FR-RS-021, SC-RS-005): the Network tab shows no `POST /recommendations` when Home mounts. The banner names the **soonest-expiring** item from already-fetched inventory.
24. Tap `Cook this →`: **exactly one** scoped `POST /recommendations` fires, with `ingredientItemIds` containing that item's id. This is the shipped `009` scoped path, reused.
25. The Tonight / Grocery run / Week affordances navigate to `/calendar` and `/grocery` (FR-RS-022).
26. With an empty inventory, empty meal plan and empty grocery list, each element shows a **calm empty state**, not a zero-filled or broken card, and the banner is replaced by its calm alternative.

**US6 — overlays and accessibility (FR-RS-023..025)**

27. Open each overlay at a touch size and at desktop: `EditItemSheet` (Kitchen → edit), `PurchasePromptSheet` (grocery → tick an ambiguous-quantity row), `MealDetailModal` (calendar → tap a planned meal), consumption review (→ **Mark cooked**), and the feedback quick-capture (bubble / `Tell us`). Each must be a **bottom sheet with a grab handle** on touch and a **centred dialog** on desktop (FR-RS-023).
28. Dismiss each by clicking the scrim **and** by pressing **Escape**; focus must return to the control that opened it (SC-RS-004).
29. With an overlay open, press `Tab` repeatedly: focus stays **inside** the panel and cycles.
30. Rotate (390×844 → 844×390) **with an overlay open**: presentation swaps sheet ↔ dialog without losing entered values or the trapped focus (spec edge case).
31. Trigger a toast (add an item): it is announced by assistive tech (`role="status"` / `aria-live="polite"`) and self-dismisses.
32. Keyboard-tab the whole app: every interactive element is a real control with an accessible name and a **terracotta focus outline** — never the default browser ring (`src/index.css:55-58`).
33. **44px audit** (FR-RS-025, SC-RS-003): measure the stepper `−`/`+`, the chip edit/delete buttons, the select-mode checkbox, the week-navigation chevrons, the pill nav items and the grocery Regenerate button. Every one ≥ 44px in **both** dimensions.
34. Enable **reduce motion** (macOS System Settings → Accessibility → Display → Reduce motion, or DevTools → Rendering → *Emulate CSS `prefers-reduced-motion`*): sheet slide-up, scrim fade and sidebar width transitions are all suppressed (FR-RS-025).
35. **Token audit** (FR-RS-024, SC-RS-008) — this must return **nothing**:

```bash
cd packages/client && grep -rEn --include='*.tsx' \
  'bg-white|text-white|border-white|bg-black|bg-cream|(bg|text|border|ring|from|to|fill|stroke|divide)-(gray|indigo|green|red|yellow|blue|slate|zinc|emerald|amber|orange|purple|pink)-[0-9]{2,3}' src/
```

Baseline before this feature: **9 files / 42 lines / 65 occurrences**. Four of those files (`WeeklyCalendar`, `CalendarSlot`, `CalendarMealCard`, `MealSlotCard` — 16 lines / 27 occurrences) are **dead code** reachable only from their own tests and are deleted rather than restyled (research D8).

## Test it

```bash
npm -w packages/client run test -- tests/hooks/useViewportClass.test.ts
npm -w packages/client run test -- tests/components/shared/Overlay.test.tsx
npm -w packages/client run test -- tests/app/nav.test.tsx
npm -w packages/client run test -- tests/views/HomePage.test.tsx
npm test                                      # full Vitest suite (coverage thresholds enforced)
npm -w packages/client run build               # next build — types + routes
npm -w packages/client run test:e2e            # Playwright, all projects
npm -w packages/client run test:e2e:run -- --project=phone-portrait   # one viewport class
bash scripts/validate-e2e.sh --no-agent        # deterministic API smoke (unchanged by this feature)
```

- **`matchMedia` must be stubbed before anything uses it** (research D9): jsdom ships no `window.matchMedia`, so `tests/setup.ts` gains the stub **and** a `setViewport(cls)` helper in RS1, before `useViewportClass()` has any consumer. Without it, every suite that renders `Nav` or `CalendarPage` throws `window.matchMedia is not a function`.
- **Unit-test per viewport class with `setViewport()`**: `setViewport('phone'); render(<CalendarPage/>)` → day strip; `setViewport('desktop')` → the 7×4 grid. The stub defaults to `'desktop'`, matching the hook's SSR default, so the other 60-odd existing test files are unaffected.
- **Playwright uses named viewport projects, not per-test `setViewportSize`** — a project sets the viewport *before first paint*, which is what the hook's mount effect reads; a mid-test resize tests the resize path instead. Only `e2e/responsive.e2e.ts` runs on all five projects; the existing 10 specs stay on `desktop` (1280×720 — already the `xl` desktop class). The one deliberate exception is the orientation-change-with-overlay-open case, which *is* a resize test.
- **New Playwright coverage is required, not optional** — CLAUDE.md §8: every new user-facing feature MUST add or extend Playwright coverage for its primary journey, and CI runs the suite on every push to `impl/nextjs`. `e2e/responsive.e2e.ts` covers: nav mode per viewport, `scrollWidth === clientWidth`, nav fixed while content scrolls, sidebar collapse + reload, day strip vs grid, sheet vs dialog, Escape + focus restoration, and the 844×390 `padding-left: 96px` assertion that proves the D1 cascade ordering.
- **Intentional test updates** (spec 010 supersedes `004` FR-UI-009 tab labels): `tests/app/nav.test.tsx` (labels `Kitchen`/`Meal plan`/`Groceries`/`Feedback` → `Home`/`Fridge`/`Plan`/`List`, plus new mode and persistence tests) and `e2e/redesign.e2e.ts:16-17`. Keep the `aria-current`, `href` and `kitchen-badge` assertions — those are behaviour, not labels.
- **Deleted with their subjects**: `tests/components/calendar/{WeeklyCalendar,CalendarMealCard,MealSlotCard}.test.tsx`.
- **Do not change `e2e/calendar-dnd.e2e.ts`.** Its `getByLabel(TILE).first()` + `boundingBox()` geometry is the guard for the one-layout rule (research D4): if a hidden phone layout ever leaks into the desktop DOM, this fails loudly instead of silently.
- **Overlay retrofit tests should stay green untouched**: `tests/components/inventory/EditItemSheet.test.tsx`, `tests/components/grocery/PurchasePromptSheet.test.tsx`, `tests/components/calendar/MealDetailModal.test.tsx`, `tests/components/consumption-review-sheet.test.tsx` all assert behaviour rather than markup — they are the proof that no flow regressed through RS6.

## Verification log

*(Per-task entries appended during implementation, mirroring specs 008/009 — baseline, per-phase TDD red→green notes, and full-gate runs.)*

## Release handoff

- [ ] Create release/version tag after review
- [ ] Build and push deployment images
- [ ] Redeploy through Portainer and verify production health checks
- [ ] Run post-deploy smoke validation against the deployed URL
- [ ] Verify all five viewport classes on real devices against the deployed URL (phone portrait/landscape, iPad portrait/landscape, desktop)
- [ ] Confirm no `004` file was edited — the superseded requirements are recorded in `spec.md` only (FR-RS-026, keeps `004` SC-UI-008 verifiable)
- [ ] Confirm zero API-surface diff under `packages/client/app/api/` (SC-RS-006)
- [ ] Queue the deferred route-rename follow-up (`/fridge`, `/plan`, `/list`, and `/` → Home)

## Gotchas

- **`phland` must be declared LAST** in `tailwind.config.ts` → `extend.screens`. A phone in landscape is ~844px wide, so `sm:` matches too; Tailwind emits `raw` screens in config order, and only last-declared wins at equal specificity. The 844×390 `padding-left: 96px` check is the assertion that catches this.
- **`extend.screens`, never `theme.screens`.** Replacing the screens map breaks the 7 responsive utilities already in the codebase (`sm:`, `lg:`, `min-[900px]:`).
- **`100dvh`, not `100vh`.** Mobile browser chrome makes `100vh` taller than the visible viewport, which pushes the nav below the fold on exactly the phones this feature is for. Tailwind 3.4 has `h-dvh` — no dependency needed.
- **`box-sizing: border-box` on the padded wrapper** is a requirement, not a nicety (design §1.4 note 2): without it the §1.3 padding adds to a `width:100%` box and causes the horizontal overflow SC-RS-001 measures.
- **Step-to-zero is the one intentional behaviour change.** Shipped code **deletes** the item at zero (`src/views/InventoryPage.tsx:87-95`; `InventoryList.tsx:9` documents "zero removes it"). FR-RS-009 says *floors at zero*, and the design specifies a zero-quantity dot state — so the row now stays and delete remains the explicit destructive action (research D10). The spec's edge-case parenthetical ("consistent with shipped behaviour") is mistaken about the current code; the requirement wins. Fix the stale `onStep` doc comment while you are there.
- **`bg-cream` does not exist.** There is no `cream` colour in `tailwind.config.ts` — the ground token is `bg` (`#f5ead8`) and the card token is `surface` (`#ebddc5`). `ConsumptionReviewSheet.tsx:31`'s `bg-cream/60` has been compiling to nothing since spec `006`. Replace with `bg-surface/60`; do **not** add a `cream` colour to the config (research D8).
- **Exactly one calendar layout mounts.** Rendering both and hiding one with CSS duplicates dnd-kit draggable ids and makes Playwright's `.first()` + `boundingBox()` resolve against a `display:none` node. Branch on `useViewportClass()` (research D4).
- **The overlay's sheet ↔ dialog swap is CSS-only, deliberately.** That is the one place *not* to branch on the viewport hook: a JS branch would remount the panel on rotation and lose both its state and its trapped focus, which the spec's orientation-change edge case forbids.
- **No nested overlays.** The consumption review is hoisted out of `MealDetailModal` up to `CalendarPage`, so only one overlay (and one focus trap) is ever open. Its spec `006` data contract — grounded clamped lines, read-only untracked ingredients, receipt-based un-cook with 409 on legacy entries — must not change.
- **UTC date handling stays as shipped.** `getWeekStart`/`getWeekDays` return UTC-midnight ISO strings and the calendar reads `getUTCDate()`/`getUTCDay()`. The handoff recommends local date parts (design §4.3 note); **do not apply it** — mixing conventions is what would introduce the off-by-one it warns about. The day strip consumes the same helpers as the grid.
- **Zero AI calls on load, on every screen** (SC-RS-005). Verify in the Network tab, not by eye. `RecommendationsPanel` has had no prefetch effect since `009` IR1 — do not add one back "to warm the cache" for Home's banner.
- **Home lives at `/home`; `/` stays the Kitchen.** Reassigning `/` would be a route rename, which FR-RS-026 forbids and reconciliation item 17 defers.
- **Mounting `GroceryListProvider` on Home triggers a rolling recompute** (spec `008` made `GET /grocery-lists/:weekStart` recompute-on-view and lazily generate). That is idempotent and identical to visiting `/grocery`, and it is not an AI call — but do not hoist the provider app-level, or the Kitchen and Calendar screens pay a request they never use.
- **Don't rebuild the nav.** The portrait pill already exists (`004` FR-UI-007) and the spec keeps it (reconciliation item 4). Extend `app/nav.tsx` — and keep the urgent-count badge (`data-testid="kitchen-badge"`), which the design does not show and SC-RS-007 protects.
- **Don't delete the `/feedback` route or its surface.** Chat, history, **Promote to development** and the pipeline view stay exactly as shipped; the overlay is *additive* quick-capture (reconciliation item 16). The route stays reachable from the overlay's "Open full feedback" link and the desktop sidebar's secondary entry (research D11).
- **Do not build the recipe modal's numbered Method steps.** The meal model carries description + verified URL only; steps are data that does not exist (reconciliation item 18).
- **No new npm dependency.** The focus trap is hand-rolled, the viewport hook is `matchMedia`, `dvh`/`raw` screens ship with Tailwind 3.4, and `lucide-react` already has every icon (research D12). Adding one should be an argued change, not a quiet drift.

### RS1 verification log (2026-07-26)

- **Baseline** (pre-RS1): lint clean; 64 files / 694 tests passing.
- **T003–T008 Foundational**: Tailwind `extend.screens` (`sm`/`min900`/`lg`/`xl` then `phland` **last**) + `maxWidth.content`; `matchMedia` stub + `setViewport()` in `tests/setup.ts`; `useViewportClass()`; **4 dead calendar components deleted** (`WeeklyCalendar`/`CalendarSlot`/`CalendarMealCard`/`MealSlotCard` + 3 test files) after proving `CalendarPage` never imported them.
  - ⚠ **Tailwind finding:** a `screens` map containing an object (the `phland` raw query) **disables arbitrary `min-[…]:`/`max-[…]:` variants build-wide**. The one shipped `min-[900px]:` utility (`InventoryPage`) was migrated to a named `min900:` screen. Use named screens only from here on.
- **T009–T016 RS1 shell**: `AppShell` (`h-dvh` flex column, `<main>` the only scroll container, nav/feedback as siblings outside it, one `box-border` padded wrapper carrying the §1.3 per-viewport padding); `layout.tsx`'s fixed `max-w-shell px-7` shell replaced; `Nav` extended to three modes (pill → rail → sidebar) with `localStorage` collapse behind a `data-nav-ready` gate; `FeedbackAffordance` per viewport.
  - **Deviation:** the design's **Home tab is deliberately omitted** until RS5 builds `/home` — RS1 ships standalone, and linking to a non-existent route would be a dead tab. `nav.tsx`/`nav.test.tsx` assert its absence; RS5 restores it.
- **Playwright (pulled forward from T063, scoped to RS1)**: five named viewport projects added; `responsive.e2e.ts` asserts nav mode + position per viewport, no horizontal page scroll on 3 screens, single-scroll-container, the **844×390 `padding-left: 96px`** `phland`-cascade proof, and desktop sidebar collapse + reload persistence. Project scoping verified: the pre-existing 22 tests still run **desktop-only** (they did not multiply ×5).
- **Results**: `npm run lint` clean · RS1 unit tests **28/28** (useViewportClass 4, AppShell 5, FeedbackAffordance 4, nav 15) · **Playwright 39 passed / 8 skipped** (skips are the intentional project guards) · `next build` clean.
- ⚠ **Full-suite note:** two full `npm test` runs showed 5 failures, **all `MongoNetworkTimeoutError`** in `tests/server/*` under load (machine load avg 8.3, lingering workers; run duration 409s vs the usual ~13s). The representative failure (`tests/server/quick-add.test.ts`) **passes in 1.74s in isolation**, and RS1 is frontend-only, so these are environmental flakes rather than regressions. Re-confirm on a quiet machine / in CI before the RS1 PR is marked ready.
- ✅ **RS1 manual test — user-verified 2026-07-26** (dev server on `localhost:3000`, real viewport resizing). Viewport behaviour across all five classes confirmed working. Two minor defects reported and **deferred by decision** (explicitly "not deal-breakers"), queued as **T061a/T061b** in RS6:
  1. **Desktop shows two feedback controls** — the sidebar `Feedback` item (`app/nav.tsx:237`, research D11) *plus* the floating `Tell us` pill; design §3 specifies one affordance per viewport.
  2. **The sidebar `Fridge Planner` wordmark is truncated** rather than wrapped at the 250px expanded width (`app/nav.tsx:134` carries `truncate`).

### RS2 verification log (2026-07-26)

- **T017** — added `packages/client/tests/components/inventory/Shelf.test.tsx` (7 tests): one card per known location with its design §4.2.3 tint (`accent2-100` fridge / `accent-100` freezer / `neutral-100` pantry), a fallback shelf for an out-of-enum location (never dropped), an `N items` count, a zero-count empty hint, and edit/delete/select-mode reachable through the shelf-wrapped `InventoryList`. Written against the not-yet-existing `Shelf` component — confirmed red (module not found) before `T021` landed, then green.
- **T018** — extended `tests/components/InventoryList.test.tsx` with two tests: a general dot+text pairing regression (expired item, stable across timezones via a 30-day offset) and a new zero-quantity case asserting the `bg-neutral-400` dot plus the corroborating `0 kg` text (FR-RS-009). The zero-quantity case failed before `ItemChip`'s zero override (`T022`) landed — the dot fell back to the expiry-status colour.
- **T019** — added a `step-to-zero floors instead of deleting` describe block to `tests/InventoryPage.test.tsx`: decrementing a 1-count item calls `updateItem(id, {quantity:0})`, never `deleteItem`, shows no "removed" toast, and the row stays rendered. Failed against the shipped `handleStep` (called `removeItem` + toast at zero) before `T024`.
- **T020** — added a regression test to `tests/components/inventory/QuickAdd.test.tsx` (actual path — `tasks.md` names `tests/components/QuickAdd.test.tsx`, but the file already lives under `inventory/` alongside the other quick-add tests, so the new test was added there): asserts the pill classes (`rounded-full`, the new placeholder copy) **and** that the shipped 005 correctable-chip preview (`I'll add:`, `category: Dairy` chip) still renders beneath it. Failed pre-`T027` on the placeholder-copy assertion.
- **T021** — built `src/components/inventory/Shelf.tsx`: a `<section aria-label="{Location} shelf">` card (role `region`), tint via a `TINT_CLASS` map keyed by the shared `LOCATIONS` values with a `neutral-100` fallback, header (`h3` name + `N item(s)` count), wrapping the shipped `InventoryList` unchanged. Also added `src/lib/locations.ts` — the single shared `LOCATIONS`/`LOCATION_LABEL`/`isKnownLocation` source research D7 calls for — and repointed `EditItemSheet.tsx`'s local copy at it (no behaviour change; its own test suite stayed green).
- **T022** — extracted the row markup into `src/components/inventory/ItemChip.tsx` (status dot + text expiry line + stepper + edit/delete/select-checkbox), keeping `InventoryList`'s own prop/handler signatures byte-identical (it now just sorts and maps to `<ItemChip>`). Added the zero-quantity override: `isZero = item.quantity <= 0` swaps the dot to `bg-neutral-400` regardless of expiry status, while the expiry-status row tint/text are untouched — corroborated by the always-visible stepper quantity text, so the zero state is never colour-only.
- **T023** — `QuantityStepper`'s `−`/`+` buttons keep their compact 30×30 visual (unchanged glyph/padding) but gained `relative before:absolute before:-inset-[7px] before:content-['']`, extending the tappable area to 44×44 (30 + 2×7) via a pseudo-element hit-area — zero new DOM nodes, zero dependencies. (No dedicated size-assertion test exists; RS6's later 44px audit explicitly excludes `QuantityStepper` since this task already covers it.)
- **T024** — `InventoryPage.handleStep` now always calls `editItem(item._id, {quantity: next})`; the `removeItem` + `"${item.name} removed"` toast branch at zero is gone. `InventoryList`'s `onStep` doc comment corrected from "zero removes it" to "floors at zero… never removed by stepping"; delete remains the one explicit destructive action.
- **T025** — deleted `src/components/inventory/LocationFilter.tsx` and `InventoryPage`'s `filter`/`visible` state; grepped the repo for `LocationFilter`/`Filter by location` post-deletion — zero remaining references. The old `visibleCount`/`totalCount` readout is superseded by each shelf's own `N items` header (D7).
- **T026** — `InventoryPage` now groups `items` via a new pure `groupByLocation()` helper (`src/lib/locations.ts` — extracted here, not left as a for-loop in the component body, to keep `InventoryPage`'s ESLint `complexity` under the ≤10 limit) into one bucket per `LOCATIONS` value plus a `fallback` bucket, and renders one `<Shelf>` per bucket inside `grid grid-cols-1 gap-3.5 sm:grid-cols-2 lg:grid-cols-3` (design §1.2's 1/2/3 column table, matching the `sm`(640)/`lg`(1024) convention already used elsewhere in this spec). The select-mode `Set<string>` wiring and spec 009's scoped-recommendations action bar are untouched — only their `LocationFilter` neighbour was removed.
- **T027** — `QuickAdd.tsx`'s input adopts the design §4.2 pill (`min-h-[50px]`, `border-[1.5px] border-divider`, `rounded-full`, `14px` neutral-600 text, 16px accent sparkle, the `Add — 2L milk expires friday…` placeholder, `max-w-[520px]` centred) and its own redundant "Add to your kitchen" heading was demoted to `sr-only` (the page has no separate title header in this scope, and the design's kicker/title is page-level, not part of the pill). `ParsePreview`, the staple chips, and all parse/correction/AI-assist logic are byte-identical.
- **Fixed along the way**: `InventoryPage`'s `handleStep` simplification plus the `groupByLocation` extraction dropped its ESLint `complexity` from 12 (over the ≤10 limit — first `next build` type-check pass) to a passing value; `ItemChip`/`InventoryList`/`Shelf`'s `onToggleSelect`/`selectMode`/`selectedIds` prop types needed explicit `| undefined` for `exactOptionalPropertyTypes` when forwarding possibly-absent values through the new `Shelf → InventoryList → ItemChip` chain — caught by `next build`'s type check, not by Vitest (jsdom doesn't type-check).
- **Consequential test update** (not itself a numbered task, but required by `T025`/`T026`): `tests/InventoryPage.test.tsx`'s `renders the location filter with an item count` test asserted `getByRole('group', {name: /filter by location/i})`, which no longer exists. Re-pointed (per research D9's guidance to re-point rather than delete a broken query) to `renders a shelf per storage location with its own item count`, asserting all three `region`-role shelves and `0 items` × 3.
- **Results**: targeted `npx vitest run --coverage=false tests/components/inventory/Shelf.test.tsx tests/components/InventoryList.test.tsx tests/InventoryPage.test.tsx tests/components/inventory/QuickAdd.test.tsx tests/components/inventory/EditItemSheet.test.tsx` → **48/48 passing**; broader `tests/{components,views,context,lib,app}` → **273/273 passing** (34 files); `npm run lint` clean; `npm -w packages/client run build` clean (type-check + static generation, 17/17 pages). Dev server on `:3000` recompiled successfully throughout and served `/` with a 200 at every check.

**RS2 visual verification (orchestrator, 2026-07-26) — caught a real regression unit tests could not.** A headless-browser check of the live dev page found **zero shelves rendering**; two distinct causes:
1. **False alarm, self-inflicted:** running `next build` while `next dev` was live overwrote the dev server's `.next`, producing `ENOENT … vendor-chunks/lucide-react.js`. Fixed by clearing `.next` and restarting. **Never run `next build` against a worktree with a live dev server** — stop dev, build, then restart.
2. **Genuine RS2 defect:** `ItemChip` was a single horizontal row (dot + name + stepper + 2 icon buttons) needing ~350px, but a 3-column shelf gives ~180px — the name collapsed to nothing, metadata wrapped one word per line, and the controls overflowed the card. All 48 unit tests passed throughout, because RTL asserts text presence, not available width. **Fixed** by restructuring the chip into a stacked card (identity row → metadata → controls with `flex-wrap`), re-verified in-browser at 1440×900 and 390×844: 3 shelves, names/expiry legible, controls inside the card, empty shelves showing their hint.
   - *Follow-up for RS6 polish:* the stacked chip is taller than the design's compact inline pill. The design's chip omits expiry/edit/delete, which FR-RS-010 requires us to retain — so stacking is the honest reconciliation, but the vertical rhythm could be tightened.

### RS3 verification log (2026-07-26)

- **T028** — added `packages/client/tests/components/calendar/DayStrip.test.tsx` (4 tests) against the not-yet-existing `DayStrip` component: a 7-column `role="tablist"`/`role="tab"` strip with exactly one `aria-selected="true"` day, a `data-testid` dot distinguishing days with meals from empty ones, `onSelect` firing with the tapped day's ISO string, and every tab a real `<button>` with an accessible name (`Select <Weekday> <N>`). Confirmed red (module not found) before `T031`.
- **T029** — extended `tests/views/CalendarPage.test.tsx` with a `CalendarPage responsive hybrid` describe block (6 tests): `setViewport('phone')` renders `[data-testid="day-strip"]` and **not** `[data-testid="week-grid"]`; `setViewport('desktop')` is the exact inverse; the empty-state copy renders at phone width for a day with nothing planned; zero `fetchRecommendations` calls on mount at either viewport (FR-RS-015); and `SuggestionsRail`'s explicit "Get suggestions" CTA is unaffected. All three layout-assertion tests failed red against the shipped unconditional grid before `T033` (the empty-state and zero-recommendation tests passed trivially even pre-refactor, since `SuggestionsRail` was already manual-trigger-only — kept as explicit regression coverage of the invariant through the refactor).
- **T030** — extracted the shipped grid **verbatim** into `packages/client/src/components/calendar/WeekGrid.tsx`: same `DndContext`, 6px `PointerSensor` activation constraint, `overflow-x-auto` + `min-w-[720px] grid-cols-7`, `PlannedMealTile`/`EmptySlotTarget`, identical Tailwind classes. `CalendarPage` retains ownership of `handleDragEnd`/`placeInto`/`getEntry`/`shiftWeek` and the UTC date helpers, passing them down as props; `WeekGrid` owns only its own `DndContext`+sensors, since drag interaction is a rendering concern of the grid itself. `dayNumber`/`dowIndex`/`todayUtcDate` moved from module-local functions in `CalendarPage.tsx` into `src/lib/date-utils.ts` (exported) so `WeekGrid` and the new `DayStrip` read the same UTC-anchored helpers — no bespoke date math anywhere (research D4). Added a `data-testid="week-grid"` hook (no visual/behavioural change) for the one-layout-mounted assertions. `tests/views/CalendarPage.test.tsx`'s pre-existing 9 tests (tap-to-place, FR-022 drag attribute, FR-024 modal, clear-without-modal) stayed green through the move, unmodified — retention proved by diff, not re-test.
- **T031** — built `src/components/calendar/DayStrip.tsx`: consumes the same `getWeekDays()` output and `dayNumber`/`dowIndex` helpers as `WeekGrid`, `role="tablist"` container, one `role="tab"` `<button>` per day (selected → `bg-accent text-bg`, unselected → `bg-surface text-ink`, per design §4.3.2), a 5px dot (`bg-accent` unselected / `bg-bg` selected / transparent when empty) gated by the caller's `hasMeals(date)` predicate.
- **T032** — built `src/components/calendar/DayPlanList.tsx`: renders the selected day's already-resolved `MealPlanEntry[]` via the shipped `PlannedMealTile` **unchanged** (same cooked-vs-planned "Cooked" badge from `entryStatus()`, same click-to-open/clear handlers) — reuse rather than a bespoke card, so the cook/un-cook contract needs no re-test. Empty day renders the design's exact copy: *"Nothing planned for this day yet — add one from the suggestions below."* A day heading (`{Weekday} {N}`) was added beyond the task's literal wording, matching design §4.3.3, for visual coherence confirmed in the browser check below.
- **T033** — `CalendarPage` now calls `useViewportClass()`; `phone = vp === 'phone' || vp === 'phone-landscape'` selects between `<DayStrip>+<DayPlanList>` and `<WeekGrid>` — **exactly one** branch renders, never both. Added `selectedDate` state defaulting to today-if-in-visible-week-else-first-day (`defaultSelectedDate()`), re-derived in a `useEffect` keyed on `currentWeekStart` so shifting weeks (`shiftWeek`) doesn't leave `selectedDate` pointing at a day no longer in `weekDays`. `hasMeals()`/`selectedDayEntries` are thin wrappers over the retained `getEntry()`, in slot order (breakfast → lunch → dinner → snack), so the phone list reads in the same order as the grid's column. `MealDetailModal` stays mounted once, below both branches, unconditionally — same component, same cook/un-cook flow, regardless of which layout opened it.
- **Results**: targeted `npx vitest run --coverage=false tests/components/calendar/DayStrip.test.tsx tests/views/CalendarPage.test.tsx tests/components/calendar/MealDetailModal.test.tsx tests/components/calendar/SuggestionsRail.test.tsx tests/lib/date-utils.test.ts tests/hooks/useViewportClass.test.ts tests/app/nav.test.tsx` → **67/67 passing**; broader `tests/` (excluding the known-flaky `tests/server/*` under load) → **716/716 passing**, no regressions from the `date-utils.ts` export change; `npm run lint` → clean, zero warnings. `npm -w packages/client run build` intentionally **not run** — a live `next dev` was serving this worktree on `:3000` (RS2's lesson: `next build` overwrites the running dev server's `.next`); build verification deferred to the orchestrator.
- **Browser check (live dev server, `:3000`, real `matchMedia`)** — a Playwright script (adapted from the RS2 helper, written to `packages/client/`, run with `node`, deleted after) hit `/calendar` at both target sizes:
  - **390×844**: `week-grid` nodes = **0**, `day-strip` nodes = **1**, `day-plan-list` nodes = **1**, `role=tab` nodes = **7**. Screenshot: day strip (Mon–Sun, selected day accent-filled with a dot on days holding meals), heading `"Saturday 25"`, empty-state prompt, `SuggestionsRail` below, bottom pill nav — no 7-column grid anywhere in the DOM.
  - **1440×900**: `week-grid` nodes = **1**, `day-strip`/`day-plan-list`/`role=tab` nodes = **0**. Screenshot: the full 7×4 grid with 3 planned meals (Mon breakfast, Wed dinner, Fri dinner) matching the phone strip's meal-dots exactly, desktop sidebar nav — no day strip anywhere in the DOM.
  - **Interaction check**: tapping a different day tab (`Select Monday 20`) at phone width swapped `DayPlanList`'s heading to `"Monday 20"` (confirming `onSelect` re-renders the list, not just the strip); clicking a planned tile at **both** phone and desktop widths opened the same `role="dialog"` `MealDetailModal` with the identical "Mark cooked" flow — proving the phone `DayPlanList` and the desktop `WeekGrid` drive the exact same cook/un-cook code path.
  - **DOM-count proof of exactly one layout**: confirmed at both sizes (counts above) — never both `week-grid` and `day-strip` present simultaneously.
- **`e2e/calendar-dnd.e2e.ts`**: left byte-for-byte unmodified, as required. Not executed in this session — `next build`/Playwright's `test:e2e` build step is explicitly the orchestrator's responsibility per this task's operational rules (running it here risked the live dev server's `.next`, the exact failure mode RS2's log documents). The verbatim extraction (`T030`) plus the passing `tests/views/CalendarPage.test.tsx` draggable-attribute assertion (`aria-roledescription="draggable"`, unchanged) are the code-level evidence retention held; the orchestrator's full-gate run is the authoritative confirmation.

**RS3 orchestrator verification (2026-07-26).** Independently confirmed in a real browser at 1440×900 and 390×844: **exactly one layout mounted** per viewport (`week-grid=1/day-strip=0` desktop; `week-grid=0/day-strip=1/tabs=7` phone — the D4 invariant), no horizontal page scroll, and **zero `POST /recommendations` on load** at either width (FR-RS-015 / spec 009 preserved). `e2e/calendar-dnd.e2e.ts` verified **byte-for-byte unmodified** (the FR-RS-013 drag-and-drop retention proof).

**Bug found and fixed during RS3 visual verification — the calendar's "today" was a day out.** The phone strip defaulted to **Sat 25** while the user's local date was **Sun 26**. Root cause: `todayUtcDate()` used `new Date().toISOString().slice(0, 10)`, which yields the *UTC* date — a day behind the user for the first hours of every day in any positive-offset timezone (09:22 AEST Sun 26 Jul = 23:22 UTC Sat 25 Jul).
- **Pre-existing**, not an RS3 regression (the helper lived in `CalendarPage.tsx:32` before the extraction) — but RS3 raised the stakes: it stopped being only a "today" marker and became the strip's **default selected day**, i.e. the user lands on yesterday and plans meals against the wrong date (fails FR-RS-012).
- **Fixed** by taking the local Y/M/D and re-projecting onto the UTC axis, exactly mirroring the server's `startOfTodayCutoff()` (spec 008 research D3) so client and server now agree on what "today" means. Two regression tests added in `tests/lib/date-utils.test.ts`, one pinning the AEST 23:22-UTC boundary with a fake clock. Re-verified in-browser: **SUN 26** selected, heading "Sunday 26".
- This is precisely the failure mode the design handoff's README warned about ("do not use `toISOString()` … shifts the calendar by a day in positive-offset timezones"). Its blanket advice was rightly *not* applied piecemeal (entry dates stay UTC-anchored), but the "which day is today" question does need the local calendar day.

### RS4 verification log (2026-07-26)

- **T034** — added `packages/client/tests/components/grocery/ProgressRing.test.tsx` (4 tests) against the not-yet-existing `ProgressRing` component: a `role="progressbar"` ring with numeric `aria-valuenow`/`aria-valuemin`/`aria-valuemax` and a visible `checked/total` figure for arbitrary `purchased.length`/`items.length` pairs, plus a no-divide-by-zero case. Confirmed red (`Cannot find module`) before `T036`. **jsdom gotcha found while writing the test**: jsdom's CSS parser (`cssstyle`) does not implement `conic-gradient()` — assigning it via an inline `style` object is silently dropped (`getAttribute('style')` reads back `null`), so `expect(ring.style.background).toContain('conic-gradient')` is untestable in this environment regardless of correct component code. Resolved by moving the gradient into a real CSS class (`.progress-ring`, `src/index.css`) driven by a `--pct` custom property set inline — custom properties bypass jsdom's value validation, so the test asserts `ring.style.getPropertyValue('--pct')` and the class name instead; the real gradient rendering is confirmed in the live-browser check below.
- **T035** — extended `packages/client/tests/pages/GroceryListPage.test.tsx` (+5 tests, one existing test rewritten for the new progress-card markup): category-grid column-count classes (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 items-start` on a `data-testid="grocery-category-groups"` node — same breakpoint mapping already shipped for Fridge shelves, `InventoryPage.tsx`); the covered week visible inside the progress card (regex-matched, since `weekLabel(currentWeekStart)` is date-derived from "today" rather than the fixture's own `weekStart`); and the three FR-RS-017/018 regression pins (below). All three column/week-label/ring tests confirmed red before `T037` (`grocery-category-groups` testid didn't exist; no week text in the progress area — it was still in the header at that point).
- **T036** — built `packages/client/src/components/grocery/ProgressRing.tsx`: 72px ring, inset 9px `--color-bg` disc (`54px`) showing `{checked}/{total}` in the heading font (design §4.4.2), fed by the exact `purchased.length`/`items.length` GroceryListPage already computed. Non-colour-only: `role="progressbar"` + `aria-label="Shopping progress"` + numeric `aria-valuenow`/`aria-valuemin`/`aria-valuemax`, paired with the always-visible text figure — satisfies the accessibility punch list without relying on the gradient fill alone.
- **T037** — `GroceryListPage.tsx`: dropped the page's own `mx-auto max-w-[720px]` wrapper (the loading/error skeleton states keep a narrower cap — no category grid renders there, so it's cosmetic only) so the category grid can reach the design's column counts inside `AppShell`'s existing 1120px desktop cap, matching the no-self-cap convention `InventoryPage.tsx` already uses for the same "1/2/3 columns" design-table row. Replaced the linear progress bar with a `max-w-[520px]` centred surface card (radius 22px, padding 18px, gap 18px per design §4.4.2) hosting `<ProgressRing>` + "In the trolley" + the **promoted** `weekLabel(currentWeekStart)` line (previously under the `<h1>` in the header — FR-RS-019, so a spec-008 day-rollover shed row reads as the rolling list it is, not a bug). Category groups now wrap in a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 items-start gap-3.5` container. `GroceryListItemRow.tsx` got the design's row metrics (`p-3 gap-[13px]`, `rounded-[20px]` category container, `border-divider` unchecked border, 42%-opacity checked name) and a **44×44px touch target** wrapping the visual 26px checkbox / remove circles (negative-margin trick, design fidelity preserved) — the accessibility punch list's touch-target minimum, previously 26px/28px. `handleTogglePurchased`, `receiptless.length`, and the checkout button's copy/logic were **not** touched — confirmed by diff: only their surrounding JSX (grid wrapper, row padding classes) changed.
- **The three tests pinning the rejected model can't creep back (spec.md reconciliation item 13)**:
  1. *`the finish-shopping count is receipt-less rows, not unchecked rows (FR-RS-018 regression pin)`* — a 3-item fixture deliberately **diverges** `receiptless.length` (2: one checked-but-receipt-less legacy row + one unchecked row) from the unchecked count (1), so a wrong "N = unchecked" implementation would render a *different, still-plausible* number (`move 1 item`) rather than failing silently on a fixture where the two counts happen to coincide — the trap flagged in this task's brief.
  2. *`ticking adds to the kitchen immediately and unticking reverses exactly, via the unchanged handleTogglePurchased path (FR-RS-017 regression pin)`* — ticks an unambiguous row and asserts `checkOffGroceryItem` (immediate add, no modal), then unticks an already-receipted row and asserts `patchGroceryItem(weekStart, itemId, { isPurchased: false })` (exact reversal) — the same two shipped-007 service calls, same argument shapes, both exercised in one test.
  3. *`completing checkout never clears the list (FR-RS-018 regression pin — reconciliation item 13)`* — after clicking "Done shopping" and awaiting the toast, asserts the item names and category headings are **still rendered** (not the empty-state copy) — the design's "move N then clear" model would fail this immediately.
- **Results**: targeted `npx vitest run --coverage=false tests/components/grocery/ProgressRing.test.tsx tests/pages/GroceryListPage.test.tsx tests/components/grocery/GroceryListItemRow.test.tsx tests/components/grocery/PurchasePromptSheet.test.tsx` → **27/27 passing** (0 failing, 0 skipped); `npm run lint` → clean, zero warnings; `tsc --noEmit` shows only pre-existing unrelated errors in other test files (none in `grocery/`, `ProgressRing.tsx`, or `GroceryListPage.tsx`). `npm -w packages/client run build` intentionally **not run** (live `next dev` serving this worktree on `:3000`; build verification is the orchestrator's).
- **Browser check (live dev server, `:3000`, real rendering)** — a Playwright script (written to `packages/client/`, run with `node`, deleted after) seeded a 6-item / 3-categories grocery list for the anonymous dev user (matching the design's own seed illustration — Lemons/Rice/Tortillas checked, Spinach/Sesame Oil/Limes not) via the live API, then hit `/grocery` at both target sizes:
  - **1440×900 (desktop)**: ring text `"3/6"`, `aria-valuenow=3`/`aria-valuemax=6`, computed `background-image` starts `conic-gradient(rgb(143, 160, 115) 50%, r…` (confirms the real browser *does* render the gradient the jsdom tests couldn't assert on directly); category grid computed 3 columns; checkout button reads `"Done shopping — move 3 items into my kitchen"` (the receipt-less count, not the 3-unchecked count, which is coincidentally also 3 on this fixture — the unit-test pin above is what actually proves the distinction); `hScroll: "no"`. Screenshot: ring + "In the trolley" + "Week of 20–26 Jul · built from your meal plan" card, 3-column Produce/Grains/Condiments layout, all matching design §4.4 layout.
  - **390×844 (phone)**: same ring/checkout values; category grid computed **1 column**; `hScroll: "no"`. Screenshot: single-column stacked categories, ring card readable at full width, bottom pill nav present, no overlap.
  - Both sizes: no horizontal page scroll, ring never colour-only (numeric figure + `aria-*` present at both viewports), 44px checkbox/remove hit targets visually confirmed (no visual size change — the design's 26px/28px circles stayed pixel-identical, only the invisible tap area grew).

**RS4 orchestrator verification (2026-07-26).** Independently confirmed in-browser: ring `3/6` with `role="progressbar"`/`aria-valuenow` + the visible figure (never colour-only), category groups **3 columns at 1440×900 → 1 at 390×844**, week context rendered ("Week of 20–26 Jul · built from your meal plan", FR-RS-019), no horizontal scroll at either width, and the checkout label driven by the **receipt-less** count. Diff-verified that `handleTogglePurchased`/`receiptless`/`checkOffGroceryItem`/`patchGroceryItem` are untouched (the only change on those lines is indentation), so spec-007 purchase semantics are preserved by construction. `npm run lint` clean; `next build` **compiled successfully** (typecheck green); targeted grocery suites 27/27.
- The three FR-RS-017/018 pins are the valuable part: one fixture deliberately makes the **receipt-less count diverge from the unchecked count**, so the design's rejected "N = unchecked" model would fail rather than silently pass on equal numbers.
- *jsdom finding worth remembering:* `cssstyle` does not implement `conic-gradient()` and silently drops any inline style containing it — the ring's gradient therefore lives in a real CSS class driven by a `--pct` custom property, and tests assert the custom property instead.

### RS5 verification log (2026-07-26)

- **T038** — added `packages/client/tests/lib/home-summary.test.ts` (11 tests) against the not-yet-existing `home-summary.ts`: `soonestExpiring()` picks the fewest-days-left item, returns `null` on an empty set and on a set with no expiry dates, `groceryProgress()` returns the `{checked,total}` pair for arbitrary purchased/total combinations (including the empty-list zero/zero case), and the four empty-state predicates (`hasNoExpiringItem`/`hasNoMealsPlanned`/`hasNoGroceryItems`/`hasNoInventoryItems`) reflect a zero count. Confirmed red (`Cannot find module`) before `T042`.
- **T039/T040** — added `packages/client/tests/views/HomePage.test.tsx` (6 tests) against the not-yet-existing `HomePage`: the four stat figures read correctly off `InventoryContext.summary`/`MealPlanContext.plan.entries.length`/the grocery checked-total pair; the banner names the correct soonest-expiring item and shows the calm "Nothing expiring soon" alternative when none exists; `Tonight`/`Grocery run` link to `/calendar`/`/grocery` and render today's dinner; every card's calm empty state with empty inventory/plan/grocery-list fixtures; and the T040 CTA test — `fetchRecommendations` **not called** on mount (flushed past any queued microtask), **called exactly once** with `['item-spinach']` after `fireEvent.click` on "Cook this →". Confirmed red (`Cannot find module '../../src/views/HomePage'`) before `T047`.
- **T041** — `packages/client/app/home/page.tsx`: net-new `/home` route wrapping `HomePage` in `GroceryListProvider`, mirroring `app/grocery/page.tsx` exactly (provider mounted on this route only, not hoisted to `app/providers.tsx`) — `/` keeps rendering `InventoryPage` unchanged (FR-RS-026).
- **T042** — `packages/client/src/lib/home-summary.ts`: `soonestExpiring()`, `groceryProgress()` (the exact `purchased.length`/`items.length` pair `GroceryListPage.tsx:139-141` already computed, extracted so both share one implementation), and the four `hasNo*` empty-state predicates — all pure, all unit-tested, zero fetch. **One deliberate correction beyond the task's literal wording, found during the browser check below**: `soonestExpiring()` excludes already-expired items (`daysLeft < 0`), matching the `dl >= 0` floor the shipped `isUrgent()` (`quick-parse.ts`, the Kitchen's "Use soon" strip) already applies — see "Bug found and fixed" below.
- **T043** — `packages/client/src/components/home/StatCard.tsx`: the four stat tiles (design §4.1.2 — accent/accent2/neutral-900/surface tone backgrounds, `36px` heading-font value + `12px` label), fed by `summary.expiringSoon`, `entries.length`, the `home-summary.ts` grocery pair, and `summary.total` — every value a direct context read, nothing re-derived client-side.
- **T044** — `packages/client/src/components/home/UseItUpBanner.tsx`: names the soonest-expiring item via `soonestExpiring()`; renders the calm "Nothing expiring soon" alternative when `item` is `null`. **One deliberate extension beyond the task's literal wording**: to make the "Cook this →" tap itself the one recommendation request (not a second click inside the mounted panel), added an optional `autoFetch` prop to `RecommendationsPanel` (default `false`, so every other caller — e.g. `InventoryPage`'s select-mode scoped action — keeps its unmodified no-mount-fetch contract, spec 009 FR-IR-001) that fires the panel's own unmodified `handleFetch()` exactly once on mount, guarded by a `useRef` (not a dependency array) so a resulting re-render can't refire it. The banner passes `ingredientItemIds={[item._id]}` + `autoFetch` only after the tap — mounting stays deferred, matching D6's "mounting the panel issues zero requests and the CTA issues exactly one."
- **T045** — `packages/client/src/components/home/TonightCard.tsx` (today's dinner entry via `entries.find(e => e.date.slice(0,10) === todayUtcDate() && e.mealType === 'dinner')`, reusing the shipped UTC-anchored `todayUtcDate()` — no new date math; "Week →" link to `/calendar`; calm empty copy) and `GroceryRunCard.tsx` (`groceryProgress()` + `hasNoGroceryItems()`, a progress bar, whole card links to `/grocery`; calm empty copy when the list has no items yet).
- **T046** — `packages/client/src/components/home/FreshPicksCard.tsx`: first 3 inventory items, status dot (reusing the same `expiryStatus`/zero-quantity dot convention as `ItemChip.tsx`) + name + quantity; calm empty copy at zero items.
- **T047** — `packages/client/src/views/HomePage.tsx`: composes all five components from `useInventory()`/`useMealPlan()`/`useGroceryList()` — no additional fetch beyond what `home/page.tsx`'s `GroceryListProvider` mount already triggers, since `InventoryProvider`/`MealPlanProvider` are already mounted app-level (`app/providers.tsx`).
- **Nav restoration (not a numbered task, but required for `/home` to be reachable)**: `app/nav.tsx`'s `TABS` array regains the `Home` (`/home`) entry RS1 deliberately omitted (its own comment said "RS5 adds it back here"); `tests/app/nav.test.tsx`'s `queryByText('Home')`-absent assertion flipped to `getByText('Home')` present with `href="/home"`. All other nav tests (positional modes, collapse persistence, `aria-current`, `kitchen-badge`) unaffected — confirmed by the full nav suite staying green (15/15).
- **Results**: targeted `npx vitest run --coverage=false tests/lib/home-summary.test.ts tests/views/HomePage.test.tsx tests/app/nav.test.tsx tests/components/RecommendationsPanel.test.tsx tests/views/CalendarPage.test.tsx tests/InventoryPage.test.tsx tests/pages/GroceryListPage.test.tsx` → **84/84 passing** (11+6+13+15+13+11+15 across the 7 files), no regressions in the panel's other callers (`InventoryPage`'s select-mode scope, the plain "Get Recommendations" flow) from the new `autoFetch` prop. `npm run lint` → clean, zero warnings (both the `packages/*/src` glob and `packages/client/app/**` checked explicitly, since the root `lint` script's glob excludes `app/`). `npx tsc --noEmit` → zero errors from any file touched this session (an `exactOptionalPropertyTypes` gap in the test fixture's `ExpiringLike`-shaped objects was found and fixed by widening `expiresAt?: string | null` to `string | null | undefined`); the remaining `tsc` errors are pre-existing and unrelated (`tests/server/*`, `tests/context/MealPlanContext.test.tsx`, etc.). `npm -w packages/client run build` intentionally **not run** — live `next dev` was serving this worktree on `:3000` (RS2's lesson); build verification deferred to the orchestrator. `git diff --stat -- packages/client/app/api packages/client/src/server` → **empty** (SC-RS-006 — no server-layer or endpoint change).
- **Bug found and fixed during the browser check — the banner could highlight an already-expired item.** First live render showed **"Bacon expired 11 days ago"** under "Use it up first" with a "Cook this →" action — nonsensical (you don't get a recipe for spoiled bacon), and inconsistent with the adjacent "expiring soon" stat card reading `0` (that stat only counts the `expiring-soon` status, not `expired`). Root cause: `soonestExpiring()` took the literal minimum `daysLeft` including negative (already-expired) values, so an expired item always wins over anything still fresh. Fixed by excluding `dl < 0` — mirroring the `dl >= 0` floor the shipped `isUrgent()` (Kitchen "Use soon" strip) already applies, so "soonest expiring" now means the same thing everywhere in the app. Two regression tests added (`excludes already-expired items`, `returns null when every item with an expiry date is already expired`). Re-verified in-browser: banner correctly picked **"Minced Pork expires in 6 days"** instead.
- **Browser check (live dev server, `:3000`, real rendering, real inventory data)** — a Playwright script (written to `packages/client/`, run with `node`, deleted after) hit `/home` at both target sizes, tracking every `POST /api/v1/recommendations` request:
  - **1440×900 (desktop)**: title `"Your kitchen at a glance"`; stat values `["0","3","3/6","11"]` (expiring soon / meals planned / groceries in / items tracked); stat grid computed **4 columns**; banner `"Minced Pork expires in 6 days"`; `Tonight` showed the empty-state copy (no dinner planned today in this dev dataset), `Grocery run` showed `3/6 in` with the fill bar, `Fresh picks` listed Bananas/Eggs/Lemons; sidebar nav labels **`["Home","Fridge","Plan","List","Feedback"]`** with `Home` active/highlighted; `hScroll: "no"`; **0** `POST /recommendations` on load.
  - **390×844 (phone)**: identical figures/banner; stat grid computed **2 columns**; bottom pill nav `["Home","Fridge","Plan","List"]` + floating feedback bubble; `hScroll: "no"`; **0** `POST /recommendations` on load.
  - **CTA interaction check**: a second script clicked "Cook this →" at 1440×900 and tracked requests before/after — **0 before, exactly 1 `POST /api/v1/recommendations` after** the single tap. Screenshot confirmed `RecommendationsPanel` mounted in place (`"From your fridge / What can I cook? / Ideas that use up your bacon and eggs first."`, "Thinking…" loading state) with no second click needed — the tap itself is the request, per D6.
  - Both sizes: no horizontal scroll; the `Home` tab renders correctly in both nav modes without disturbing the other three labels/hrefs/badge.

**RS5 orchestrator verification (2026-07-26).** `/home` renders the four figures, the use-it-up banner and the three summary cards; `next build` registers `○ /home` (6.04 kB) and compiles clean; lint clean.
- **SC-RS-005 (zero AI calls on load) confirmed in-browser at three places**: `/home` desktop = 0, `/home` phone = 0, **and `/` (Kitchen) = 0** — the last one matters because RS5 introduced an `autoFetch` prop on `RecommendationsPanel`; it defaults to `false`, is passed **only** by `UseItUpBanner`, and additionally requires `scoped`, so spec 009's no-auto-load guarantee cannot leak back. Tapping "Cook this →" fires exactly one scoped request.
- **SC-RS-006 (no new endpoint) proven by diff**: `git diff -- packages/client/app/api packages/client/src/server` is **empty**. All four figures are reads of already-fetched data (`summary.expiringSoon`, `summary.total`, `plan.entries.length`, and `groceryProgress()` extracted from `GroceryListPage` so both share one implementation).
- **Stat-vs-API consistency checked atomically** (same page context reading the DOM and `GET /inventory`): UI `expiringSoon=0` vs API `expiringSoon=0` → match. An earlier screenshot showed `1`; that was live data changing between observations (the verification runs themselves seed/mutate inventory), not a wiring bug.
- Banner correctly names the soonest **non-expired** item — a bug found during the subagent's own browser check (it initially picked an already-expired item, since the raw minimum days-left includes negatives) and fixed to match the shipped `isUrgent()` floor, with two regression tests.
- **Home tab restored to the nav** now that `/home` exists, reversing RS1's deliberate omission; `tests/app/nav.test.tsx` flipped from asserting Home absent to asserting it present with `href="/home"`.
